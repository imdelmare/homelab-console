import{_ as a,o as s,c as t,a2 as i}from"./chunks/framework.BxL9yGGP.js";const u=JSON.parse('{"title":"Providers","description":"","frontmatter":{},"headers":[],"relativePath":"providers.md","filePath":"providers.md","lastUpdated":null}'),n={name:"providers.md"};function o(r,e,d,l,c,p){return s(),t("div",null,[...e[0]||(e[0]=[i(`<h1 id="providers" tabindex="-1">Providers <a class="header-anchor" href="#providers" aria-label="Permalink to &quot;Providers&quot;">​</a></h1><p>A <strong>provider</strong> is the adapter layer between a tool and a real homelab system. Tools never talk to Proxmox, OPNsense, etc. directly — they call a provider, which owns connection details, auth, and error handling for that one system.</p><h2 id="provider-abstraction" tabindex="-1">Provider abstraction <a class="header-anchor" href="#provider-abstraction" aria-label="Permalink to &quot;Provider abstraction&quot;">​</a></h2><p>Providers live under <code>apps/api/app/providers/</code>:</p><ul><li><code>base.py</code> — the provider interface every adapter implements (connect, health/status check, and the operations backing that provider&#39;s tools).</li><li><code>registry.py</code> — maps <code>provider_id</code> to a provider instance/class, used by the execution core to resolve which provider backs a given tool.</li><li><code>httpclient.py</code> — the shared HTTP client base most providers build on: applies the <a href="#error-taxonomy">error taxonomy</a> and the TLS policy in one place, so individual adapters don&#39;t reimplement either.</li></ul><p>Providers are read-only by default. Exact write capabilities exist only when they are separately governed and approval-bound. There are 17 built-in provider instances across 16 integration families (the two FritzBox entries are separate instances); allowlisted API-ready instances can be added by config:</p><table tabindex="0"><thead><tr><th>Directory</th><th><code>provider_id</code></th><th>Talks to</th></tr></thead><tbody><tr><td><code>proxmox/</code></td><td><code>proxmox</code></td><td>Proxmox VE API (API token)</td></tr><tr><td><code>pbs/</code></td><td><code>pbs</code></td><td>Proxmox Backup Server API (API token)</td></tr><tr><td><code>opnsense/</code></td><td><code>opnsense</code></td><td>OPNsense API (key/secret)</td></tr><tr><td><code>mikrotik/</code></td><td><code>mikrotik</code></td><td>RouterOS REST API</td></tr><tr><td><code>homeassistant/</code></td><td><code>homeassistant</code></td><td>Home Assistant REST API (long-lived token)</td></tr><tr><td><code>frigate/</code></td><td><code>frigate</code></td><td>Frigate HTTP API</td></tr><tr><td><code>adguard/</code></td><td><code>adguard</code></td><td>AdGuard Home control API</td></tr><tr><td><code>nextcloud/</code></td><td><code>nextcloud</code></td><td><code>status.php</code> + OCS APIs (app password)</td></tr><tr><td><code>nutups/</code></td><td><code>nutups</code></td><td>Network UPS Tools <code>upsd</code> text protocol</td></tr><tr><td><code>asterisk/</code></td><td><code>asterisk</code></td><td>AMI, whitelisted read actions only</td></tr><tr><td><code>uptimekuma/</code></td><td><code>uptimekuma</code></td><td><code>/metrics</code> (API key) + status pages</td></tr><tr><td><code>emqx/</code></td><td><code>emqx</code></td><td>EMQX v5 management API</td></tr><tr><td><code>cloudflaretunnel/</code></td><td><code>cloudflaretunnel</code></td><td>Official Cloudflare Tunnel and connector APIs</td></tr><tr><td><code>api_ready/</code></td><td>declared instance ID</td><td>Allowlisted API drivers, including the official Cloudflare Tunnel API</td></tr><tr><td><code>vps/</code></td><td><code>vps</code></td><td>VPS reachability, Glances, WireGuard and deploy status endpoints</td></tr><tr><td><code>zerotier/</code></td><td><code>zerotier</code></td><td>ZeroTier Central Legacy v1 API (personal API token)</td></tr><tr><td><code>fritzbox/</code></td><td><code>fritzbox_primary</code>, <code>fritzbox_secondary</code></td><td>TR-064/UPnP (one instance per box)</td></tr></tbody></table><h2 id="shared-transport-contracts-and-special-providers" tabindex="-1">Shared transport contracts and special providers <a class="header-anchor" href="#shared-transport-contracts-and-special-providers" aria-label="Permalink to &quot;Shared transport contracts and special providers&quot;">​</a></h2><p>The standard HTTP/JSON providers are Proxmox, PBS, OPNsense, MikroTik, Home Assistant, Frigate, AdGuard, Nextcloud, Uptime Kuma, EMQX, Cloudflare Tunnel, ZeroTier and the VPS Glances integration. They share <code>BaseJsonClient</code>, including TLS enforcement, timeouts, error mapping and explicit per-call response modes (<code>json</code>, <code>text</code> or <code>auto</code>). A successful text/HTML response cannot silently pass through a JSON operation.</p><h3 id="opnsense-wake-on-lan" tabindex="-1">OPNsense Wake-on-LAN <a class="header-anchor" href="#opnsense-wake-on-lan" aria-label="Permalink to &quot;OPNsense Wake-on-LAN&quot;">​</a></h3><p><code>opnsense.wol.wake</code> is a disabled-by-default write capability defined by ADR 0007. It requires the optional OPNsense <code>os-wol</code> plugin and accepts only a configured <code>target_id</code>. Homelab Console resolves that id to an OPNsense saved-host UUID and calls only <code>POST /api/wol/wol/set</code>. MAC addresses, interfaces, broadcast addresses and arbitrary API paths are never caller inputs, and the plugin&#39;s <code>wakeall</code> action is not exposed.</p><p>A successful result confirms only that OPNsense reported sending the magic packet; WoL has no reliable power-state acknowledgement. ADR 0008 records the completed <code>operator-workstation</code> drill and activates the exact tool id through governance. The confirmed plugin surface uses <code>searchHost</code>/<code>setHost</code> naming for host administration and <code>set</code> for sending to one saved host; Homelab Console exposes only the latter.</p><h3 id="opnsense-gateway-transitions-and-egress" tabindex="-1">OPNsense gateway transitions and egress <a class="header-anchor" href="#opnsense-gateway-transitions-and-egress" aria-label="Permalink to &quot;OPNsense gateway transitions and egress&quot;">​</a></h3><p><code>opnsense.gateway.failover</code> and <code>opnsense.gateway.restore</code> accept no caller input. They resolve only the primary and backup UUID/name pairs declared under <code>providers.opnsense.gateway_failover</code>, apply the transition in a safe order and require gateway-status plus kernel default-route read-back. ADR 0010 records the successful failover and separately approved restore drill.</p><p>The same scoped network-action identity supplies only the exact WoL, gateway configuration/status and routing-table privileges needed by these tools. They never fall back to the general OPNsense reader credentials.</p><p><code>opnsense.egress.switch</code> remains disabled. ADR 0011 records that changing gateway upstream flags moved the firewall&#39;s kernel default route but an existing LAN firewall policy-routing rule kept client traffic on primary ISP. Any successor must pin that declared firewall rule by UUID, keep all gateway and WireGuard endpoint boundaries closed, and verify the public country using the fixed <code>network.egress.status</code> observation before activation.</p><p><code>GET /api/provider-definitions</code> exposes their non-secret catalog metadata: driver, configuration key names, tool capabilities and typed observation IDs. <code>GET /api/observations</code> executes narrow read tools through the shared execution core and projects normalized results into capability health.</p><p>The standard TCP text transport is implemented by <code>BaseTcpTextClient</code>. It owns bounded line/response reads, connection lifecycle, operation deadlines and the same normalized reachability/error vocabulary. It does <strong>not</strong> expose commands. Two explicit drivers currently use it:</p><ul><li><code>asterisk_ami_v1</code> — AMI banner/login, key-value blocks and event streams;</li><li><code>nut_upsd_v1</code> — NUT commands, <code>BEGIN/END</code> lists and quoted values.</li></ul><p>AMI actions and NUT operations remain separately allowlisted. They share no wire commands, parser or authentication state machine, and no REST/MCP caller can provide a TCP payload.</p><p>These integrations still remain outside the shared transport catalogs:</p><ul><li>FritzBox (SOAP/TR-064)</li><li>the future source-routed primary ISP probe</li></ul><p>Their future observation model can change independently. Asterisk and NUT also retain protocol-specific observations/normalizers even though their connection transport is now shared.</p><h2 id="cloudflare-tunnel-api-provider" tabindex="-1">Cloudflare Tunnel API provider <a class="header-anchor" href="#cloudflare-tunnel-api-provider" aria-label="Permalink to &quot;Cloudflare Tunnel API provider&quot;">​</a></h2><p>The canonical <code>cloudflaretunnel</code> provider uses the fixed official Cloudflare API origin with mandatory TLS verification. It replaces the legacy public URL probes and accepts no URL, account or tunnel input from REST/MCP callers:</p><div class="language-yaml vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">providers</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  cloudflaretunnel</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    account_id</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">0123456789abcdef0123456789abcdef</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    tunnel_ids</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      - </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">11111111-2222-4333-8444-555555555555</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    timeout_seconds</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">8</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br></div></div><p>The required credential is an account-scoped API token with only <code>Cloudflare Tunnel Read</code>. It is not the <code>eyJ...</code> runtime token used to start cloudflared:</p><div class="language-yaml vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">cloudflaretunnel</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  bearer_token</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">EXAMPLE-READ-ONLY-TOKEN</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>The read-only tools are <code>cloudflare.tunnels.status</code>, <code>cloudflare.connectors.list</code> and <code>cloudflare.summary</code>. Connector UUIDs, connection UUIDs, origin IPs, colo names, metadata and raw Cloudflare payloads are discarded. The provider owns tunnel-to-edge health and feeds the <code>cloudflaretunnel.tunnel</code> topology observation plus the dedicated <code>cloudflare.tunnel</code> watcher. Uptime Kuma remains the canonical source for individual public application availability.</p><h2 id="configuration-driven-api-ready-instances" tabindex="-1">Configuration-driven API-ready instances <a class="header-anchor" href="#configuration-driven-api-ready-instances" aria-label="Permalink to &quot;Configuration-driven API-ready instances&quot;">​</a></h2><p>Small systems implementing the narrow <code>json_health_v1</code> contract can be added without copying a client:</p><div class="language-yaml vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">api_provider_instances</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  - </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">id</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">example_service</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    name</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">Example Service</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    driver</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">json_health_v1</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    base_url</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">https://example-service.internal</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    verify_tls</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    timeout_seconds</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">5</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br></div></div><p>After restart this adds the provider and one read-only tool, <code>example_service.health.status</code>. The driver always requests <code>GET /health</code>, accepts only a JSON object with a known <code>status</code> value, and returns only <code>instance_id</code>, normalized <code>status</code> and <code>reported_status</code>. REST/MCP callers cannot supply a URL or path, and arbitrary response fields are discarded. An optional bearer token lives under <code>example_service.bearer_token</code> in the gitignored secrets file.</p><p>The shared <code>cloudflare_tunnel_v1</code> transport can also back an additional, independent compatibility instance when a separate account/tunnel must remain isolated from the canonical provider:</p><div class="language-yaml vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">api_provider_instances</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  - </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">id</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">cloudflare_home</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    name</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">Cloudflare Home Tunnel</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    driver</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">cloudflare_tunnel_v1</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    account_id</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">0123456789abcdef0123456789abcdef</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    tunnel_id</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">11111111-2222-4333-8444-555555555555</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    timeout_seconds</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">8</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br></div></div><p>Its <code>cloudflare_home.tunnel.status</code> tool always calls the fixed official Cloudflare API origin and the declared account/tunnel path. TLS verification cannot be disabled. The required <code>cloudflare_home.bearer_token</code> secret must be an account-scoped token with only <code>Cloudflare Tunnel Read</code>; raw Cloudflare errors, metadata, connector IPs and connection payloads are discarded. The normalized output contains only tunnel identity, state, configuration source and active/inactive timestamps.</p><p>The API driver reports whether the connector is healthy at Cloudflare&#39;s edge. It deliberately does not replace Uptime Kuma, which verifies whether each published application actually responds.</p><p>These are compatibility profiles, not generic HTTP providers. A system needing richer or vendor-specific capabilities still gets dedicated narrow tools.</p><h2 id="zerotier-central-legacy-provider" tabindex="-1">ZeroTier Central Legacy provider <a class="header-anchor" href="#zerotier-central-legacy-provider" aria-label="Permalink to &quot;ZeroTier Central Legacy provider&quot;">​</a></h2><p>The built-in <code>zerotier</code> provider uses the fixed official Legacy v1 origin, <code>https://api.zerotier.com/api/v1</code>, with TLS verification always enabled. Its stable internal driver identity is <code>zerotier_central_legacy_v1</code>; the public provider and tool IDs do not need to change if the lab later migrates to the new Central v2 API.</p><p>Declare only the networks the console may observe:</p><div class="language-yaml vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">providers</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  zerotier</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    network_ids</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      - </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">0123456789abcdef</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    timeout_seconds</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">8</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    offline_after_seconds</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">600</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # Empty means this is an on-demand VPN and may legitimately be idle.</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    required_online_member_ids</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: []</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br></div></div><p>Keep the personal Legacy Central token in the gitignored secrets file:</p><div class="language-yaml vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">zerotier</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  api_token</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">EXAMPLE-READ-ONLY-TOKEN</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>The four read tools are <code>zerotier.status</code>, <code>zerotier.networks.list</code>, <code>zerotier.members.list</code> and <code>zerotier.summary</code>. They cannot enumerate networks outside <code>network_ids</code>. Output includes normalized network metadata, member authorization, freshness and assigned ZeroTier IPs; physical addresses, account details, authorization headers and raw vendor payloads are discarded. The <code>zerotier.members</code> capability observation drives the dedicated topology node. Authorized laptops and phones may normally be offline, so their stale state remains an informational metric. Only IDs explicitly listed in <code>required_online_member_ids</code> affect observation, summary and watcher health; use this only for genuine always-on anchors. With an empty list, a successful Central read is healthy even when the network has no online members. A missing or offline required member degrades the summary, makes the topology capability unavailable and creates a watcher incident.</p><h2 id="response-normalization" tabindex="-1">Response normalization <a class="header-anchor" href="#response-normalization" aria-label="Permalink to &quot;Response normalization&quot;">​</a></h2><p>Raw vendor payloads never leave a provider. Each provider follows the same layout (see <code>proxmox/</code> as the reference):</p><ul><li><code>models.py</code> — Pydantic models with <code>extra=&quot;ignore&quot;</code>; these are the only shapes exposed to the frontend and to model providers.</li><li><code>normalizers.py</code> — pure functions converting raw vendor payloads into those models, dropping unknown fields and converting annotated strings (&quot;12.3 ms&quot;, &quot;0.0 %&quot;) into plain numbers.</li><li><code>tools.py</code> — the provider class plus tool implementations: call the client, normalize, return the normalized shape.</li></ul><p>This keeps tool output stable when a vendor adds fields, and prevents leaking internal detail (paths, coordinates, credential fragments) that vendor status endpoints like to include.</p><h2 id="status-vocabulary" tabindex="-1">Status vocabulary <a class="header-anchor" href="#status-vocabulary" aria-label="Permalink to &quot;Status vocabulary&quot;">​</a></h2><p>Every provider reports one of these statuses, plus the timestamp of its last successful observation (persisted on <code>ProviderConfiguration</code>):</p><table tabindex="0"><thead><tr><th>Status</th><th>Meaning</th></tr></thead><tbody><tr><td><code>healthy</code></td><td>Reachable and responding normally</td></tr><tr><td><code>degraded</code></td><td>Reachable but with reduced confidence (partial data, slow, non-fatal errors)</td></tr><tr><td><code>unreachable</code></td><td>Configured, but the network/endpoint could not be reached</td></tr><tr><td><code>unavailable</code></td><td>Not usable right now for a reason other than reachability (e.g. not configured in this lab, disabled)</td></tr><tr><td><code>misconfigured</code></td><td>Configuration present but invalid (bad URL, missing required field)</td></tr><tr><td><code>unknown</code></td><td>No observation yet, or status could not be determined</td></tr></tbody></table><p>The <strong>last successful observation timestamp</strong> is persisted even while a provider is currently degraded/unreachable, so the UI and Telegram <code>/status</code> can show &quot;last seen X ago&quot; rather than just a boolean.</p><h2 id="error-taxonomy" tabindex="-1">Error taxonomy <a class="header-anchor" href="#error-taxonomy" aria-label="Permalink to &quot;Error taxonomy&quot;">​</a></h2><p>Provider calls normalize failures into one of these error kinds before they reach the execution core&#39;s output/redaction stage:</p><table tabindex="0"><thead><tr><th>Error</th><th>Meaning</th></tr></thead><tbody><tr><td><code>configuration_missing</code></td><td>Provider has no endpoint/configuration at all (e.g. no entry in <code>HOMELAB_CONFIG_PATH</code>)</td></tr><tr><td><code>credentials_missing</code></td><td>Configuration present but required credential fields are empty</td></tr><tr><td><code>auth_failed</code></td><td>Credentials present but rejected by the target system</td></tr><tr><td><code>timeout</code></td><td>Call exceeded the tool&#39;s <code>timeout_seconds</code></td></tr><tr><td><code>tls_error</code></td><td>TLS handshake/certificate validation failed</td></tr><tr><td><code>unreachable</code></td><td>Network-level failure to reach the target</td></tr><tr><td><code>permission_denied</code></td><td>Authenticated, but the credential lacks permission for the operation</td></tr><tr><td><code>invalid_response</code></td><td>Target responded, but the response didn&#39;t match the expected shape</td></tr><tr><td><code>degraded</code></td><td>Call succeeded but with reduced confidence/partial data</td></tr></tbody></table><p>Tools surface these as normalized, redacted error results — never a raw stack trace or raw provider error body, which could otherwise leak internal detail or credential fragments.</p><h2 id="adding-a-provider-—-step-by-step" tabindex="-1">Adding a provider — step by step <a class="header-anchor" href="#adding-a-provider-—-step-by-step" aria-label="Permalink to &quot;Adding a provider — step by step&quot;">​</a></h2><p>First decide whether the system implements one of the exact API-ready contracts above. If it does, add one server-side instance entry and restart. If it needs another endpoint, authentication flow or output shape, use the dedicated-driver process below; never loosen an existing driver into an arbitrary HTTP client. For a new line-oriented TCP protocol, build a dedicated driver on <code>BaseTcpTextClient</code>; keep its command allowlist and parser inside that driver.</p><ol><li>Create <code>apps/api/app/providers/&lt;name&gt;/</code> implementing the interface in <code>base.py</code> (endpoint setup from <code>HOMELAB_CONFIG_PATH</code>, credentials from <code>SECRETS_PATH</code>, a status/health method, and one method per operation the provider will back). Build the client on <code>httpclient.py</code> unless the target isn&#39;t HTTP.</li><li>Map every failure mode to the <a href="#error-taxonomy">error taxonomy</a> above — don&#39;t let a raw exception escape the adapter.</li><li>Add <code>models.py</code> + <code>normalizers.py</code> following the <a href="#response-normalization">normalization layout</a> — no tool returns a raw vendor payload.</li><li>Register the provider in <code>registry.py</code> under a stable <code>provider_id</code>.</li><li>Add the provider&#39;s non-secret endpoint shape to <code>config/homelab.example.yml</code> and its credential shape to <code>config/secrets.local.example.yml</code> (placeholder values only — see <a href="./security"><code>security.md</code></a> on secret handling).</li><li>Implement a status/health check that reports one of the six <a href="#status-vocabulary">status values</a> and persists the last successful observation timestamp.</li><li>Add tools for it (see next section) — a provider with zero tools is inert.</li></ol><h2 id="adding-a-tool-—-step-by-step" tabindex="-1">Adding a tool — step by step <a class="header-anchor" href="#adding-a-tool-—-step-by-step" aria-label="Permalink to &quot;Adding a tool — step by step&quot;">​</a></h2><ol><li>Decide the dotted <code>tool_id</code>, e.g. <code>opnsense.firewall.rules.list</code>.</li><li>Define Pydantic input and output schemas — reject extra fields on input.</li><li>Set <code>mode</code> (<code>read</code>/<code>write</code>), <code>risk</code> (<code>low</code>/<code>medium</code>/<code>high</code>/<code>critical</code>), <code>timeout_seconds</code>, and <code>requires_confirmation</code>. <ul><li>Remember policy this milestone: all <strong>write</strong> tools must ship <code>enabled=false</code>; <code>high</code>/<code>critical</code> risk tools also ship <code>enabled=false</code> until approval flows are explicitly reviewed for them. See <a href="./security#risk-levels-and-tool-policy"><code>security.md</code></a>.</li></ul></li><li>Implement the tool&#39;s call into the provider method it wraps — a tool is a thin, schema-validated wrapper around one provider operation, not a place to add ad hoc logic that bypasses the provider abstraction.</li><li>Register the tool in the tool registry so it&#39;s discoverable via both REST and MCP (see <a href="./mcp#tool-discovery-and-execution"><code>mcp.md</code></a>).</li><li>Confirm the full pipeline in <a href="./architecture#shared-execution-core"><code>architecture.md</code></a> applies automatically (it does, by construction) — no extra wiring is needed for auth, redaction, or audit.</li></ol><h2 id="proxmox-setup" tabindex="-1">Proxmox setup <a class="header-anchor" href="#proxmox-setup" aria-label="Permalink to &quot;Proxmox setup&quot;">​</a></h2><p>Proxmox is read-only by default. The two LXC power tools designed under operator-reviewed design record remain disabled unless their exact ids are operator-activated in <code>APPROVED_WRITE_TOOLS</code> after the required live drill.</p><h3 id="_1-create-a-dedicated-api-token-in-the-proxmox-ui" tabindex="-1">1. Create a dedicated API token in the Proxmox UI <a class="header-anchor" href="#_1-create-a-dedicated-api-token-in-the-proxmox-ui" aria-label="Permalink to &quot;1. Create a dedicated API token in the Proxmox UI&quot;">​</a></h3><p>Do not reuse the <code>root@pam</code> password, and do not reuse a token from another tool.</p><ol><li>In the Proxmox VE web UI: <strong>Datacenter → Permissions → API Tokens → Add</strong>.</li><li>Choose (or create) a dedicated user for this integration, e.g. <code>homelab-console@pve</code> — avoid <code>root@pam</code>.</li><li>Give the token a clear Token ID, e.g. <code>homelab-console@pve!api</code>.</li><li><strong>Uncheck &quot;Privilege Separation&quot;</strong> only if you understand the tradeoff; leaving it checked (default) means the token&#39;s permissions are governed by the role you grant it below, which is the recommended, least-privilege path.</li><li>Copy the generated token secret immediately — Proxmox shows it once.</li></ol><h3 id="_2-grant-least-privilege-access-pveauditor" tabindex="-1">2. Grant least-privilege access (PVEAuditor) <a class="header-anchor" href="#_2-grant-least-privilege-access-pveauditor" aria-label="Permalink to &quot;2. Grant least-privilege access (PVEAuditor)&quot;">​</a></h3><ol><li><strong>Datacenter → Permissions → Add → User/Group/API Token Permission</strong>.</li><li>Assign the built-in <strong><code>PVEAuditor</code></strong> role (read-only cluster/node/guest visibility) to the token, scoped at the level the tools need (typically <code>/</code> for full read visibility across nodes/guests, or narrower if you want to limit which nodes are visible).</li><li>Do <strong>not</strong> grant <code>PVEAdmin</code> or <code>PVEVMAdmin</code>. The read tool set (<code>proxmox.version</code>, <code>proxmox.cluster.status</code>, <code>proxmox.topology</code>, <code>proxmox.nodes.list</code>, <code>proxmox.resources.list</code>, <code>proxmox.guests.list</code>, <code>proxmox.vms.list</code>, <code>proxmox.lxc.list</code>, <code>proxmox.storage.list</code>, <code>proxmox.tasks.failed</code>) needs audit visibility only.</li><li>If and only if the LXC write drill is being prepared, create a separate power token with privilege separation enabled. Add a narrowly scoped custom role containing only <code>VM.Audit</code> and <code>VM.PowerMgmt</code> to both its backing user and token on the intended <code>/vms/&lt;vmid&gt;</code> paths. Do not grant power management at <code>/</code> or <code>/vms</code> when per-container ACLs are workable. The backing user must remain enabled because Proxmox rejects its tokens when it is disabled; omit a password to prevent interactive password login.</li></ol><h3 id="_3-configure-the-provider" tabindex="-1">3. Configure the provider <a class="header-anchor" href="#_3-configure-the-provider" aria-label="Permalink to &quot;3. Configure the provider&quot;">​</a></h3><p>In <code>config/homelab.local.yml</code> (gitignored — copy from <code>config/homelab.example.yml</code>), keep the endpoint and connection policy:</p><div class="language-yaml vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">proxmox</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  base_url</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">https://EXAMPLE-PVE-HOST:8006</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  verify_tls</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  critical_lxc_vmids</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: []</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><p>List DNS, networking, storage and Homelab Console containers under <code>critical_lxc_vmids</code>. A matching <code>proxmox.lxc.shutdown</code> request displays a prominent warning but still requires the normal single-use approval.</p><p>In <code>config/secrets.local.yml</code> (gitignored — copy from <code>config/secrets.local.example.yml</code>), keep only credentials:</p><div class="language-yaml vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">proxmox</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  api_token_id</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">console-reader@pve!homelab-console</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  api_token_secret</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">EXAMPLE-TOKEN-SECRET</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  power_api_token_id</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">console-lxc-power@pve!homelab-console</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  power_api_token_secret</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">EXAMPLE-POWER-TOKEN-SECRET</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><p>The reader token resolves inventory and performs post-action read-back. The power token is used only for the narrow LXC start/shutdown request and its task status. Never replace the reader credentials with the scoped power token.</p><h3 id="_4-verify-tls-guidance" tabindex="-1">4. <code>verify_tls</code> guidance <a class="header-anchor" href="#_4-verify-tls-guidance" aria-label="Permalink to &quot;4. \`verify_tls\` guidance&quot;">​</a></h3><ul><li><strong>Preferred</strong>: <code>verify_tls: true</code>. Use a certificate Proxmox&#39;s HTTPS endpoint presents that your app trusts (a real CA-issued cert, or your internal CA&#39;s cert installed in the app&#39;s trust store).</li><li><strong>Trusted LAN/VPN exception</strong>: set <code>ALLOW_INSECURE_LOCAL_TLS=true</code> in the runtime and <code>verify_tls: false</code> only for providers addressed by private IP. The app logs a loud warning and still rejects public IPs and public hostnames. Prefer installing your internal CA and removing the exception when practical.</li></ul><h2 id="optional-providers" tabindex="-1">Optional providers <a class="header-anchor" href="#optional-providers" aria-label="Permalink to &quot;Optional providers&quot;">​</a></h2><p>Providers without an endpoint in <code>HOMELAB_CONFIG_PATH</code> report as <code>unavailable</code>. Providers with an endpoint but missing credentials report as <code>misconfigured</code>. This distinction lets the UI separate &quot;not installed in this lab&quot; from &quot;configured target but missing secret&quot;.</p><h2 id="uptime-kuma-as-availability-layer" tabindex="-1">Uptime Kuma as availability layer <a class="header-anchor" href="#uptime-kuma-as-availability-layer" aria-label="Permalink to &quot;Uptime Kuma as availability layer&quot;">​</a></h2><p>Provider health answers whether the console can use a system API; service availability is a separate signal. A topology node can bind an exact Kuma monitor name with <code>availability_monitor</code>. The observations API then emits <code>uptimekuma.monitor.&lt;node-id&gt;</code>, and Kuma incidents are attributed to that declared service node.</p><p>Choose one Telegram owner for those monitors:</p><ul><li>console-owned: keep <code>uptimekuma.monitors</code> enabled and disable direct Kuma notifications for the same monitors;</li><li>Kuma-owned: keep direct Kuma notifications and disable the console <code>uptimekuma.monitors</code> watcher. The read-only availability projection still works.</li></ul><p>Running both notification paths for the same monitor intentionally produces duplicates and is unsupported.</p>`,85)])])}const m=a(n,[["render",o]]);export{u as __pageData,m as default};
