import{_ as a,o as s,c as t,a2 as n}from"./chunks/framework.DYd_c05j.js";const u=JSON.parse('{"title":"MCP Adapter","description":"","frontmatter":{},"headers":[],"relativePath":"mcp.md","filePath":"mcp.md","lastUpdated":null}'),i={name:"mcp.md"};function o(r,e,l,d,c,p){return s(),t("div",null,[...e[0]||(e[0]=[n(`<h1 id="mcp-adapter" tabindex="-1">MCP Adapter <a class="header-anchor" href="#mcp-adapter" aria-label="Permalink to &quot;MCP Adapter&quot;">​</a></h1><p><code>apps/mcp</code> exposes Homelab Console over <a href="https://modelcontextprotocol.io" target="_blank" rel="noreferrer">Model Context Protocol</a>. MCP clients can call the same infrastructure tool plane used by REST, and can also operate persistent tasks through narrow task tools. They do not get shell, SSH, arbitrary HTTP, raw API forwarding or provider-specific shortcuts.</p><p>The current legacy Fixer operating policy and OpenCode bootstrap prompt are documented in isolated worker documentation. ADR 0024 defines its migration to capability-gated, vendor-neutral remediation workers. The core now implements per-client identity, operator assignment, durable jobs, <code>tasks.worker.next/renew/finish</code>, lease recovery and fencing. Worker task mutations, approval requests and task-bound infrastructure calls all require the same active lease; generic claim/create/reopen remain unavailable. The first conversion requires an explicit irreversible-conversion confirmation; removing the capability later disables eligibility but preserves the unique worker principal for that bearer token.</p><h2 id="same-execution-core-different-transport" tabindex="-1">Same execution core, different transport <a class="header-anchor" href="#same-execution-core-different-transport" aria-label="Permalink to &quot;Same execution core, different transport&quot;">​</a></h2><p>The MCP adapter is a thin transport layer. Every infrastructure tool call it receives is forwarded to the same <code>execute_tool</code> function described in <a href="./architecture#shared-execution-core"><code>architecture.md</code></a> that backs the REST API. There is no separate MCP-only implementation of tool logic, policy, or redaction — MCP is another door into the same house, not a different house.</p><p>Concretely, this means an MCP client gets exactly the same:</p><ul><li>tool registry (only tools marked <code>enabled</code>; write tools are enabled only through the operator-approved <code>APPROVED_WRITE_TOOLS</code> allowlist, ADR 0004),</li><li>input schema validation (extra fields rejected),</li><li>risk-policy enforcement (low/medium/high/critical),</li><li>per-invocation, single-use, input-bound approval for every write or high-risk tool,</li><li>timeouts,</li><li>output normalization and secret redaction,</li><li>audit logging (<code>AuditEvent</code>) and task-event recording.</li></ul><p>For infrastructure tool calls, <code>task_id</code> is MCP metadata. The adapter removes it from the provider input before validation and passes it separately to <code>execute_tool</code>; provider tools never receive a caller-supplied <code>task_id</code> inside their own payload.</p><h2 id="transport" tabindex="-1">Transport <a class="header-anchor" href="#transport" aria-label="Permalink to &quot;Transport&quot;">​</a></h2><p>The adapter supports two MCP transports:</p><ul><li><strong>stdio</strong>: launched as a local subprocess by the MCP client.</li><li><strong>streamable HTTP</strong>: an optional listener bound to loopback or a trusted LAN interface and exposed publicly only through the declared Cloudflare Tunnel. Every MCP request requires a registered bearer token. When the connector is not colocated, a firewall ACL must restrict origin access to its source.</li></ul><p>The native <code>homelab-console-mcp.service</code> starts the optional HTTP listener for local/LAN use. Stdio remains client-started. See <code>scripts/</code> for live runtime helpers and <code>apps/mcp/requirements.txt</code> for dependencies.</p><h2 id="authentication" tabindex="-1">Authentication <a class="header-anchor" href="#authentication" aria-label="Permalink to &quot;Authentication&quot;">​</a></h2><p>Stdio clients authenticate as the configured agent identity <strong><code>MCP_AGENT_ID</code></strong> (see <code>.env.example</code>). <code>MCP_AGENT_ID</code> must be <code>claude</code>, <code>fixer</code>, <code>codex</code>, <code>cline</code>, <code>opencode</code>, or the vendor-neutral <code>worker</code>; it is read from the process environment/configuration at startup and is not accepted from individual tool calls.</p><p>HTTP clients authenticate with per-client bearer tokens. The token itself determines the client identity (<code>claude</code>, <code>fixer</code>, <code>codex</code>, <code>cline</code>, <code>opencode</code>, or <code>worker</code>), label, fingerprint, dashboard row, and <code>last_seen_at</code>.</p><h2 id="http-onboarding" tabindex="-1">HTTP Onboarding <a class="header-anchor" href="#http-onboarding" aria-label="Permalink to &quot;HTTP Onboarding&quot;">​</a></h2><p>The operator-managed HTTP onboarding flow is the same for Codex, Claude, Cline and OpenCode:</p><ol><li>In the <strong>MCP Clients</strong> section, the operator selects the agent and enters its label and host fingerprint.</li><li>The dashboard calls <code>POST /api/mcp/pairing/start</code>.</li><li>The backend sends an approve/deny prompt to the authorized Telegram chat.</li><li>The dashboard polls <code>POST /api/mcp/pairing/consume</code> after approval.</li><li>The dashboard shows the plaintext token exactly once.</li><li>The operator stores that token on the client, which connects to <code>https://mcp.example.com/mcp/</code> with <code>Authorization: Bearer &lt;token&gt;</code>.</li></ol><p>Use these values per client:</p><table tabindex="0"><thead><tr><th>Client</th><th><code>agent_id</code></th><th>Example <code>client_label</code></th><th>Example <code>host_fingerprint</code></th></tr></thead><tbody><tr><td>Codex</td><td><code>codex</code></td><td><code>Codex operator-workstation</code></td><td><code>operator-workstation</code></td></tr><tr><td>Claude</td><td><code>claude</code></td><td><code>Claude operator-workstation</code></td><td><code>operator-workstation</code></td></tr><tr><td>Fixer</td><td><code>fixer</code></td><td><code>Fixer</code></td><td><code>opencode-fixer</code></td></tr><tr><td>Cline</td><td><code>cline</code></td><td><code>Cline operator-workstation</code></td><td><code>operator-workstation</code></td></tr><tr><td>OpenCode</td><td><code>opencode</code></td><td><code>OpenCode operator-workstation</code></td><td><code>operator-workstation</code></td></tr><tr><td>Remediation adapter</td><td><code>worker</code></td><td><code>Remediation worker</code></td><td><code>worker-host</code></td></tr></tbody></table><p>Use <code>worker</code> for a provider-independent remediation adapter. Before the operator grants <code>task-worker.v1</code>, that registration is quarantined to task reads: it cannot claim work, mutate tasks, call infrastructure tools or acquire leases. The first grant permanently converts it to its per-client worker principal. Provider-specific worker registrations remain supported for compatibility, but the generic identity avoids core changes for a new engine.</p><p><code>client_label</code> is what the dashboard shows. <code>host_fingerprint</code> should be stable for that machine; the hostname is enough for this homelab setup.</p><p>The <strong>MCP Clients</strong> section can start this flow directly: choose Codex, Claude, Fixer, Cline, OpenCode or Remediation worker, enter the label and host fingerprint, click <strong>Start Telegram pairing</strong>, then approve the Telegram request. The UI checks approval immediately and then every 2 seconds until the request is approved or expires; <strong>Check approval</strong> can also be clicked manually. When approved, the UI shows the plaintext bearer token once; paste it into that client&#39;s MCP HTTP configuration. The same panel shows recent pairing requests with status, delivery result, expiry/decision timestamps and deciding actor, but never exposes pairing secrets or hashes. All pairing timestamps are displayed in <code>Europe/Rome</code>. A request still stored as <code>pending</code> is displayed as <strong>expired</strong> once its <code>expires_at</code> deadline has passed.</p><p>The pairing interface, including menus, accessibility labels, status, and error messages, uses US English. This affects only the operational UI, not agent task content or stored historical/user content.</p><h3 id="prompt-for-new-clients" tabindex="-1">Prompt For New Clients <a class="header-anchor" href="#prompt-for-new-clients" aria-label="Permalink to &quot;Prompt For New Clients&quot;">​</a></h3><p>Give this instruction to a fresh Codex, Claude, Cline or OpenCode session after starting pairing from the dashboard:</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Connect to Homelab Console MCP over its Cloudflare Tunnel HTTPS endpoint.</span></span>
<span class="line"><span></span></span>
<span class="line"><span>The operator performs Telegram pairing in the MCP Clients section and will</span></span>
<span class="line"><span>provide the per-client hmc_... token exactly once. Do not call the internal</span></span>
<span class="line"><span>/api/mcp/pairing endpoints yourself.</span></span>
<span class="line"><span></span></span>
<span class="line"><span>MCP URL: https://mcp.example.com/mcp/</span></span>
<span class="line"><span>Transport: streamable HTTP</span></span>
<span class="line"><span>Header: Authorization: Bearer &lt;token&gt;</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Do not use a shared static token.</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br></div></div><h3 id="pairing-boundary" tabindex="-1">Pairing boundary <a class="header-anchor" href="#pairing-boundary" aria-label="Permalink to &quot;Pairing boundary&quot;">​</a></h3><p>Start and consume pairing through the operator dashboard. The narrow onboarding endpoints are reachable without a web session so a new client can bootstrap, but they are not MCP tools: start is rate-limited, Telegram approval is mandatory, and consume requires the one-time pairing secret returned only to the initiating browser. Pairing history and all client administration remain authenticated; capability grants additionally require CSRF protection. Never expose the pairing secret to an MCP client or treat Telegram approval as a reusable credential.</p><h3 id="client-configuration" tabindex="-1">Client Configuration <a class="header-anchor" href="#client-configuration" aria-label="Permalink to &quot;Client Configuration&quot;">​</a></h3><p>All clients use the same MCP HTTP endpoint:</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>https://mcp.example.com/mcp/</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>The token must be sent as:</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Authorization: Bearer hmc_...</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>For Codex, configure the token as an environment variable and point the MCP server at it:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> HOMELAB_MCP_TOKEN</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;hmc_...&quot;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">codex</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> mcp</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> add</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> homelab-console</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --url</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> https://mcp.example.com/mcp/</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  --bearer-token-env-var</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> HOMELAB_MCP_TOKEN</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">codex</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><p>For OpenCode, keep the token outside the checked-in configuration:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> HOMELAB_MCP_TOKEN</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;hmc_...&quot;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><div class="language-json vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;$schema&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;https://opencode.ai/config.json&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;mcp&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;homelab-console&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;type&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;remote&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;url&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;https://mcp.example.com/mcp/&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;enabled&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;oauth&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">false</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;headers&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">        &quot;Authorization&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Bearer {env:HOMELAB_MCP_TOKEN}&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br><span class="line-number">14</span><br></div></div><p>For Claude and Cline, use the same URL and bearer token in the client&#39;s MCP HTTP configuration UI or JSON config. If a client cannot complete onboarding itself, run the two manual pairing calls, copy the <code>hmc_...</code> token from the approved <code>consume</code> response, and paste it into that client&#39;s MCP HTTP config.</p><h3 id="dashboard-and-revocation" tabindex="-1">Dashboard And Revocation <a class="header-anchor" href="#dashboard-and-revocation" aria-label="Permalink to &quot;Dashboard And Revocation&quot;">​</a></h3><p>The dashboard lists every registered MCP client separately. A client is considered <code>online</code> only when its <code>last_seen_at</code> is within the UI freshness window; otherwise it shows as <code>idle</code> even though the token is still valid.</p><p>Use the dashboard revoke action to disable one client without affecting the others. Revoked clients immediately fail MCP HTTP authentication with <code>401</code>. The <strong>MCP Clients</strong> section also contains copyable onboarding prompts for Codex, Claude, Cline and OpenCode, all using the same Telegram pairing flow and HTTP endpoint. The dashboard can start/check a pairing request, revoke a client, or rotate one client&#39;s bearer token. Rotation updates the stored token hash in place, invalidates the previous token immediately, and returns the new plaintext token only in that one response. After revocation, <strong>Forget</strong> permanently removes the client registration and token hash. The backend rejects Forget for active clients; pairing history and audit events remain available.</p><h3 id="troubleshooting" tabindex="-1">Troubleshooting <a class="header-anchor" href="#troubleshooting" aria-label="Permalink to &quot;Troubleshooting&quot;">​</a></h3><ul><li><code>401 unauthorized</code> from <code>https://mcp.example.com/mcp/</code>: the client did not send the bearer token, used the wrong token, or the token was revoked.</li><li><code>202 pairing_not_approved</code> from <code>/api/mcp/pairing/consume</code>: approve the Telegram prompt, then retry.</li><li><code>409 pairing_consumed</code>: the one-time pairing was already exchanged for a token; start a new pairing if the token was lost.</li><li>No Telegram message: check <code>TELEGRAM_BOT_TOKEN</code>, <code>TELEGRAM_ALLOWED_CHAT_ID</code>, and the API logs.</li><li>Dashboard shows <code>idle</code>: make the client perform any MCP request; <code>last_seen_at</code> updates when a registered bearer token is accepted.</li></ul><p>Stdio clients can also use <strong>MCP client registration</strong>:</p><ol><li>First startup has no valid local client token.</li><li>The adapter creates a pairing request with agent id, client label and host fingerprint.</li><li>The backend sends an approve/deny prompt to the authorized Telegram chat.</li><li>After approval, the adapter consumes the pairing request and receives a token once.</li><li>The adapter stores the token locally with <code>0600</code> permissions.</li><li>Later startups validate the token and update <code>last_seen_at</code>.</li></ol><p>The backend stores only a token hash and a short token hint, never the token plaintext. Operators can list, revoke and forget registered clients through <code>/api/mcp/clients</code>; forgetting is restricted to clients that are already revoked.</p><p>Useful environment variables:</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">MCP_AGENT_ID</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">codex</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">MCP_CLIENT_TOKEN_PATH</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">~/.config/homelab-console/mcp-codex.token</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">MCP_CLIENT_LABEL</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Codex workstation&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">MCP_PAIRING_TIMEOUT_SECONDS</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">300</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">python</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> apps/mcp/server.py</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><p>Claude, Codex, Cline and OpenCode are <strong>external MCP clients</strong> — they connect to this adapter over stdio or HTTP, exactly like any other MCP client. The control panel never starts, supervises, or calls out to an LLM API on their behalf; nothing in the task lifecycle makes an Anthropic/OpenAI/any model API call. Telegram free-text replies and authenticated REST conversation endpoints use a separate, deliberately narrow conversation service — see <a href="./architecture"><code>architecture.md</code></a> — which is unrelated to this adapter and to task ownership.)</p><h2 id="tool-discovery-and-execution" tabindex="-1">Tool discovery and execution <a class="header-anchor" href="#tool-discovery-and-execution" aria-label="Permalink to &quot;Tool discovery and execution&quot;">​</a></h2><ul><li><strong>Discovery</strong>: the adapter lists the currently enabled tools from the tool registry, with their id, description, and input schema, so an MCP client can present them to a model or a human without hardcoding the catalog. It also exposes typed <code>tasks.*</code> tools for task state and <code>approvals.*</code> tools for the write-approval flow. Enabled write tools (necessarily ADR-approved) are listed with an extra optional <code>approval_id</code> input.</li><li><strong>Write approvals</strong>: an agent requests approval for one exact invocation with <code>approvals.request(tool_id, input, task_id?)</code>; the operator decides on Telegram (inline buttons) or in the console Approvals section. The agent polls <code>approvals.get</code> and, once approved, calls the tool with that <code>approval_id</code> and the same input. Approvals are single-use, input-bound (SHA-256 of the validated input) and expire after <code>APPROVAL_TTL_SECONDS</code> (default 600).</li><li><strong>Execution</strong>: a call names a <code>tool_id</code> and input payload; the adapter validates the shape at the MCP layer and then hands off to <code>execute_tool</code>, which re-validates against the Pydantic schema and runs the full pipeline described in <a href="./architecture"><code>architecture.md</code></a>.</li><li>Results returned to the MCP client are the same normalized, redacted output the REST API would return for the same tool call.</li></ul><h2 id="persistent-task-tools" tabindex="-1">Persistent task tools <a class="header-anchor" href="#persistent-task-tools" aria-label="Permalink to &quot;Persistent task tools&quot;">​</a></h2><p>Task tools modify only Homelab Console application state, so they never require an infrastructure write approval.</p><p>MCP clients should load the <code>homelab_task_workflow</code> prompt at session start and treat it as operating instructions for task work. It states the required lifecycle explicitly, including the common completion path:</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>tasks.claim</span></span>
<span class="line"><span>tasks.set_status(status=&quot;investigating&quot;)</span></span>
<span class="line"><span>...</span></span>
<span class="line"><span>tasks.update_summary</span></span>
<span class="line"><span>tasks.complete</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><p><code>tasks.complete</code> is valid only from <code>investigating</code>; a direct <code>claimed -&gt; completed</code> attempt is rejected by the backend. If a task is still <code>claimed</code>, first call <code>tasks.set_status(status=&quot;investigating&quot;)</code> with the latest <code>expected_version</code>, then call <code>tasks.complete</code>.</p><p>Read tools:</p><ul><li><code>tasks.list</code></li><li><code>tasks.get</code></li><li><code>tasks.context</code></li><li><code>tasks.events.list</code></li><li><code>tasks.findings.list</code></li><li><code>tasks.checks.list</code></li></ul><p>Write tools:</p><ul><li><code>tasks.create</code></li><li><code>tasks.claim</code></li><li><code>tasks.release</code></li><li><code>tasks.set_status</code></li><li><code>tasks.update_summary</code></li><li><code>tasks.add_note</code></li><li><code>tasks.add_finding</code></li><li><code>tasks.resolve_finding</code></li><li><code>tasks.add_check</code></li><li><code>tasks.complete_check</code></li><li><code>tasks.skip_check</code></li><li><code>tasks.complete</code></li><li><code>tasks.reopen</code></li></ul><p><code>tasks.context</code> returns the backend-compiled agent brief: task identity, linked incident, provider ids/status, bounded recent findings/checks/events/tool invocations, recent watcher runs, recommended read-only tool allowlist, budget, stop conditions and deterministic <code>recommended_next_step</code> (a fixed string chosen from task status/checks/findings — no LLM call involved). The MCP adapter only exposes this read model; it does not compile context itself.</p><h2 id="task-ownership" tabindex="-1">Task ownership <a class="header-anchor" href="#task-ownership" aria-label="Permalink to &quot;Task ownership&quot;">​</a></h2><p>Once a task is <code>assigned_agent</code>-claimed, only that agent may mutate it. <code>tasks.release</code>, <code>tasks.set_status</code>, <code>tasks.update_summary</code>, <code>tasks.add_note</code>, <code>tasks.add_finding</code>, <code>tasks.resolve_finding</code>, <code>tasks.add_check</code>, <code>tasks.complete_check</code>, <code>tasks.skip_check</code> and <code>tasks.complete</code> all check that the calling agent (<code>agent:claude</code>, <code>agent:fixer</code>, <code>agent:codex</code>, <code>agent:cline</code>, <code>agent:opencode</code>, or the authenticated client&#39;s <code>agent:worker:&lt;client-id&gt;</code> principal) matches <code>assigned_agent</code>; a mismatch — including an agent trying to act on a task nobody has claimed yet — fails with <code>not_task_owner</code>. A human operator acting through the authenticated web UI (REST, <code>kind=&quot;user&quot;</code>) or Telegram always bypasses this check (administrative override); <code>tasks.claim</code> itself stays atomic and idempotent for the same agent re-claiming its own task. Worker principals cannot use <code>tasks.claim</code>; operator assignment and a current lease are required before their task mutations or task-bound infrastructure calls are accepted.</p><p><code>tasks.release</code> works from any active assigned status — <code>claimed</code>, <code>investigating</code>, <code>waiting_operator</code>, <code>blocked</code> — and always returns the task to <code>open</code> with <code>assigned_agent</code> and <code>claimed_at</code> cleared, recording both the canonical <code>task.status_changed</code> transition and <code>task.released</code> with the handoff details. Claims likewise record <code>task.status_changed</code> plus <code>task.claimed</code>; every task status change passes through the same transition helper.</p><p>A <code>completed</code>/<code>cancelled</code> task is immutable until explicitly reopened: summary/note/finding/check mutations and infrastructure tool calls made with its <code>task_id</code> fail with <code>task_not_active</code>, including operator/service callers. A successful task-linked tool call on an active task bumps its <code>version</code> (in addition to <code>last_activity_at</code>).</p><p>To avoid an extra <code>tasks.get</code> round trip after every infrastructure tool call, the response to any call made with <code>task_id</code> includes a <code>task_version</code> field — the task&#39;s current version after that call, whether it succeeded or was rejected (e.g. <code>not_task_owner</code>, <code>task_not_active</code>). Use it directly as <code>expected_version</code> on the next <code>tasks.*</code> mutation for that task.</p><h2 id="agent-handoff" tabindex="-1">Agent handoff <a class="header-anchor" href="#agent-handoff" aria-label="Permalink to &quot;Agent handoff&quot;">​</a></h2><p>The intended workflow, first agent:</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>tasks.list</span></span>
<span class="line"><span>tasks.claim</span></span>
<span class="line"><span>tasks.context</span></span>
<span class="line"><span>tasks.set_status(investigating)</span></span>
<span class="line"><span>lab.summary(task_id=...)</span></span>
<span class="line"><span>tasks.add_finding</span></span>
<span class="line"><span>tasks.add_check</span></span>
<span class="line"><span>tasks.update_summary</span></span>
<span class="line"><span>tasks.release(handoff_summary=&quot;...&quot;)</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br></div></div><p>Second agent:</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>tasks.claim</span></span>
<span class="line"><span>tasks.context</span></span>
<span class="line"><span>tasks.complete_check</span></span>
<span class="line"><span>tasks.update_summary</span></span>
<span class="line"><span>tasks.complete</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><p>Concretely, a typical Claude → Codex handoff:</p><ol><li>Claude claims the task with <code>tasks.claim</code>, sets <code>tasks.set_status</code> to <code>investigating</code>, and runs infrastructure tools (e.g. <code>lab.summary</code>) with <code>task_id</code> set so invocations are linked.</li><li>Claude records findings/checks/summary with <code>tasks.add_finding</code>, <code>tasks.add_check</code>, <code>tasks.update_summary</code>.</li><li>Claude releases the task with <code>tasks.release</code> and a non-empty <code>handoff_summary</code> (or leaves it in <code>waiting_operator</code> for a human, via <code>tasks.set_status</code>).</li><li>Codex claims the same task with <code>tasks.claim</code> — it&#39;s <code>open</code> again after release, so <code>tasks.set_status(investigating)</code> first if it needs to run more tools.</li><li>Codex reads <code>tasks.context</code> (summary, pending checks, open findings, <code>recommended_next_step</code>) and continues the work: <code>tasks.complete_check</code>, <code>tasks.update_summary</code>, then <code>tasks.complete</code>.</li></ol><h2 id="what-it-cannot-bypass" tabindex="-1">What it cannot bypass <a class="header-anchor" href="#what-it-cannot-bypass" aria-label="Permalink to &quot;What it cannot bypass&quot;">​</a></h2><p>Because MCP terminates in the same <code>execute_tool</code> core:</p><ul><li>it cannot call a disabled tool,</li><li>it cannot run a write tool outside the operator-approved <code>APPROVED_WRITE_TOOLS</code> allowlist, and even an approved write tool needs a fresh operator decision for every single invocation — agents can request approvals, never grant them,</li><li><code>tasks.*</code> writes only affect task state and still go through typed schemas,</li><li>it cannot skip approval for a <code>requires_confirmation</code> tool,</li><li>it cannot receive unredacted secrets — the same centralized redaction applies,</li><li>it cannot invent a tool outside the registry — only declared, schema-validated tools exist,</li><li>it cannot reach a provider directly — it only ever reaches providers through a tool, never a raw connection,</li><li>it cannot mutate a task owned by a different agent, or run infrastructure tools against a task it doesn&#39;t own once that task has been claimed.</li></ul><p>In short, the MCP adapter changes <em>how</em> a caller reaches the tool plane, not <em>what</em> the tool plane will do.</p>`,79)])])}const k=a(i,[["render",o]]);export{u as __pageData,k as default};
