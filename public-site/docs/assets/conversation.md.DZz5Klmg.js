import{_ as t,o as a,c as o,a2 as s}from"./chunks/framework.DYd_c05j.js";const u=JSON.parse('{"title":"Conversation Service","description":"","frontmatter":{},"headers":[],"relativePath":"conversation.md","filePath":"conversation.md","lastUpdated":null}'),i={name:"conversation.md"};function n(r,e,d,l,c,h){return a(),o("div",null,[...e[0]||(e[0]=[s(`<h1 id="conversation-service" tabindex="-1">Conversation Service <a class="header-anchor" href="#conversation-service" aria-label="Permalink to &quot;Conversation Service&quot;">​</a></h1><p>The Conversation Service is the lightweight natural-language layer used by Telegram chat, the Operations panel and authenticated REST endpoints. A Chat section is not currently registered in the web console. It is intentionally separate from the MCP agent workflow: it helps the operator ask questions, run a small set of read-oriented summary tools, and create or update Homelab Console tasks, but it never becomes an autonomous agent.</p><h2 id="scope" tabindex="-1">Scope <a class="header-anchor" href="#scope" aria-label="Permalink to &quot;Scope&quot;">​</a></h2><p>The service can:</p><ul><li>understand short operator requests in natural language;</li><li>choose from a fixed allowlist of summary/task tools;</li><li>call summary tools through <code>execute_tool</code>;</li><li>read tasks, create tasks, and update task summaries through the task service;</li><li>propose task creation and wait for explicit operator confirmation;</li><li>write conversation messages, model usage, and estimated cost to the database.</li></ul><p>The service cannot:</p><ul><li>execute shell commands;</li><li>use SSH;</li><li>call arbitrary URLs;</li><li>modify provider configuration;</li><li>run infrastructure write tools;</li><li>bypass <code>execute_tool</code>, validation, policy, redaction, audit, or task ownership.</li></ul><h2 id="channels" tabindex="-1">Channels <a class="header-anchor" href="#channels" aria-label="Permalink to &quot;Channels&quot;">​</a></h2><p>Current channels use the same backend service:</p><ul><li><strong>Authenticated REST</strong>: <code>POST /api/conversations/message</code>, authenticated with the normal session + CSRF flow. No Chat section is currently registered in the web console, but the endpoint is already available for authenticated clients.</li><li><strong>Telegram free chat</strong>: ordinary non-command text uses a one-decision, tool-free response contract. It receives no tool catalog or current task and cannot create or update tasks. When live data is needed, the reply directs the operator to the Operations panel.</li><li><strong>Telegram Operations</strong>: slash commands remain deterministic (<code>/menu</code>, <code>/status</code>, <code>/tasks</code>, <code>/incidents</code>, <code>/watchers</code>, <code>/mcp</code>, <code>/provider</code>, <code>/approve</code>, <code>/deny</code>). Inline callback buttons provide the operator dashboard. Non-command The Operations panel exposes fixed summary shortcuts and a one-shot <code>ForceReply</code> question. Fixed shortcuts execute their declared summary tool first and need only one model pass to explain the result. An arbitrary live question is bound to the authorized user and chat by a five-minute, single-use nonce; only that reply enters the governed tool-selection flow. Task proposals use a separate inline button and short-lived nonce.</li></ul><p>This channel-neutral shape is deliberate so a future chat UI can reuse the same service contract without changing the model/tool policy.</p><h2 id="language-policy" tabindex="-1">Language policy <a class="header-anchor" href="#language-policy" aria-label="Permalink to &quot;Language policy&quot;">​</a></h2><p>Assistant replies, task titles, goals, and summaries generated from a conversation, and conversational analysis of Telegram images use Italian. Audio transcription preserves the language actually spoken; it is not translated before being stored or passed as bounded context. Existing conversation history and operator-provided content are never translated. This is separate from the US English web, Telegram operations, notification, and MCP-pairing interfaces.</p><h2 id="telegram-photos-and-voice-messages" tabindex="-1">Telegram photos and voice messages <a class="header-anchor" href="#telegram-photos-and-voice-messages" aria-label="Permalink to &quot;Telegram photos and voice messages&quot;">​</a></h2><p>The authorized Telegram operator can send a photo, voice message, or audio attachment. The API obtains the exact <code>file_id</code> from Telegram, resolves it through Telegram&#39;s fixed <code>getFile</code> API, and enforces configured byte and duration limits before analysis.</p><p>Media analysis is local-only:</p><ul><li>JPEG, PNG, and WebP images are sent to the configured Ollama model;</li><li>voice/audio is decoded in-process and normalized to mono PCM WAV at 16 kHz, then sent to Gemma through Ollama&#39;s multimodal input;</li><li>only the bounded description or transcript enters the Conversation Service;</li><li>original bytes and base64 are never stored in conversation history, audit metadata, or model usage records;</li><li>media is never sent to the Luna fallback. If Ollama is unavailable, the bot returns a controlled attachment-analysis error.</li></ul><p>PyAV performs audio decoding with fixed output parameters. No shell command, caller-supplied path, URL, codec, or conversion option is executed. Defaults are 8 MB per image, 10 MB per audio file, and 120 seconds per audio message, controlled by <code>TELEGRAM_MEDIA_*</code> settings.</p><h2 id="model-contract" tabindex="-1">Model Contract <a class="header-anchor" href="#model-contract" aria-label="Permalink to &quot;Model Contract&quot;">​</a></h2><p>The service supports the OpenAI Responses API and a fixed Ollama <code>/api/chat</code> endpoint. Ollama can be selected as the primary model with Luna as the automatic fallback. Both adapters enforce the same strict structured-output schema. The model receives only a compact context:</p><ul><li>the current user message;</li><li>a short bounded conversation history;</li><li>the allowed tool catalog;</li><li>the current task, when attached;</li><li>compact redacted tool results from the previous step.</li></ul><p>It does not receive raw provider credentials, raw provider config files, full audit dumps, unbounded history, or arbitrary provider payloads.</p><p>Governed Operations questions must return this structured shape:</p><div class="language-json vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;assistant_reply&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;...&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;tool_calls&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [],</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;create_task&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">false</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;task_title&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;task_goal&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;update_task_id&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;update_task_summary&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;needs_clarification&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">false</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br></div></div><p>Free chat and fixed Operations shortcuts instead use a strict reply-only schema containing <code>assistant_reply</code>. The backend ignores any model attempt to introduce tools or task mutations outside the governed Operations question contract.</p><p>The backend, not the model, decides what is actually executed. Invalid tools are reported as <code>tool_not_allowed</code>; tool inputs are still validated by the execution core or task service.</p><h2 id="allowed-tools" tabindex="-1">Allowed Tools <a class="header-anchor" href="#allowed-tools" aria-label="Permalink to &quot;Allowed Tools&quot;">​</a></h2><p>The allowlist is intentionally narrow:</p><ul><li><code>lab.summary</code></li><li><code>lab.alerts.recent</code></li><li><code>lab.network.summary</code></li><li><code>lab.storage.summary</code></li><li><code>lab.security.summary</code></li><li><code>lab.automation.summary</code></li><li><code>tasks.list</code></li><li><code>tasks.get</code></li><li><code>tasks.create</code></li><li><code>tasks.update_summary</code></li></ul><p><code>lab.storage.summary</code> includes NUT UPS state when the <code>nutups</code> provider is configured, so Luna can include power/battery findings without broadening the allowlist to arbitrary provider calls.</p><p>Summary tools go through <code>app.tools.execution.execute_tool</code> with <code>source=&quot;conversation&quot;</code>. Task tools are thin calls into <code>app.services.tasks_service</code>, which owns validation, audit, redaction, and task-state rules for Homelab Console&#39;s own database.</p><h2 id="limits" tabindex="-1">Limits <a class="header-anchor" href="#limits" aria-label="Permalink to &quot;Limits&quot;">​</a></h2><p>The runtime limits are configurable through environment variables:</p><table tabindex="0"><thead><tr><th>Setting</th><th style="text-align:right;">Default</th><th>Meaning</th></tr></thead><tbody><tr><td><code>CONVERSATION_ENABLED</code></td><td style="text-align:right;"><code>true</code></td><td>Master gate; when false, no conversation, media analysis, Task Router worker or model-assisted incident matching is invoked</td></tr><tr><td><code>CONVERSATION_PROVIDER</code></td><td style="text-align:right;"><code>ollama</code></td><td><code>ai_manager</code> for inventory-bound LAN inference with Luna fallback, <code>ollama</code> for local inference, <code>openai</code> for Luna only, or <code>opencode_go</code> for direct OpenCode Go inference</td></tr><tr><td><code>CONVERSATION_MODEL</code></td><td style="text-align:right;"><code>gpt-5.6-luna</code></td><td>OpenAI model used for conversation routing</td></tr><tr><td><code>OPENCODE_GO_API_KEY</code></td><td style="text-align:right;">empty</td><td>Server-side OpenCode Go API key; never caller-visible or persisted</td></tr><tr><td><code>OPENCODE_GO_CHAT_MODEL</code></td><td style="text-align:right;"><code>deepseek-v4-flash</code></td><td>Closed-enum lightweight Chatter model for the fixed Go endpoint</td></tr><tr><td><code>OPENCODE_GO_ROUTER_MODEL</code></td><td style="text-align:right;"><code>deepseek-v4-pro</code></td><td>Closed-enum asynchronous Task Router model</td></tr><tr><td><code>OPENCODE_GO_MAX_ATTEMPTS</code></td><td style="text-align:right;"><code>3</code></td><td>Bounded retries for transient transport/status failures</td></tr><tr><td><code>CONVERSATION_REASONING_EFFORT</code></td><td style="text-align:right;"><code>low</code></td><td>Reasoning effort sent to the model</td></tr><tr><td><code>CONVERSATION_MAX_TURNS</code></td><td style="text-align:right;"><code>4</code></td><td>Bounded history window</td></tr><tr><td><code>CONVERSATION_MAX_TOOL_CALLS</code></td><td style="text-align:right;"><code>3</code></td><td>Maximum tool calls in one turn</td></tr><tr><td><code>CONVERSATION_MAX_OUTPUT_TOKENS</code></td><td style="text-align:right;"><code>600</code></td><td>Maximum model output tokens</td></tr><tr><td><code>CONVERSATION_TIMEOUT_SECONDS</code></td><td style="text-align:right;"><code>60</code></td><td>OpenAI request timeout</td></tr><tr><td><code>AI_MANAGER_HOST_ID</code></td><td style="text-align:right;"><code>ai-host</code></td><td>Inventory host used by the LAN AI manager</td></tr><tr><td><code>AI_MANAGER_PORT</code></td><td style="text-align:right;"><code>8080</code></td><td>Port that must also be declared in the host&#39;s <code>check_ports</code></td></tr><tr><td><code>AI_MANAGER_MODEL</code></td><td style="text-align:right;"><code>Qwen3.5-4B-Q8_0</code></td><td>llama.cpp model alias</td></tr><tr><td><code>AI_MANAGER_CONNECT_TIMEOUT_SECONDS</code></td><td style="text-align:right;"><code>2</code></td><td>Fast LAN connection timeout</td></tr><tr><td><code>AI_MANAGER_TIMEOUT_SECONDS</code></td><td style="text-align:right;"><code>60</code></td><td>LAN inference timeout</td></tr><tr><td><code>AI_MANAGER_FAILURE_COOLDOWN_SECONDS</code></td><td style="text-align:right;"><code>90</code></td><td>Shared cooldown before retrying the LAN manager</td></tr><tr><td><code>OLLAMA_BASE_URL</code></td><td style="text-align:right;"><code>http://127.0.0.1:11434</code></td><td>Fixed server-side Ollama endpoint; override locally for a dedicated model host</td></tr><tr><td><code>OLLAMA_MODEL</code></td><td style="text-align:right;"><code>gemma4-e4b-agentic:latest</code></td><td>Local primary model</td></tr><tr><td><code>OLLAMA_CONNECT_TIMEOUT_SECONDS</code></td><td style="text-align:right;"><code>2</code></td><td>Fast local connection timeout</td></tr><tr><td><code>OLLAMA_TIMEOUT_SECONDS</code></td><td style="text-align:right;"><code>90</code></td><td>Local inference timeout</td></tr><tr><td><code>OLLAMA_FAILURE_COOLDOWN_SECONDS</code></td><td style="text-align:right;"><code>90</code></td><td>Skip Ollama briefly after a failure</td></tr><tr><td><code>OLLAMA_KEEP_ALIVE</code></td><td style="text-align:right;"><code>10m</code></td><td>Keep the local model loaded between requests</td></tr><tr><td><code>TELEGRAM_MEDIA_ENABLED</code></td><td style="text-align:right;"><code>true</code></td><td>Allow authorized Telegram photo/audio analysis</td></tr><tr><td><code>TELEGRAM_MEDIA_MAX_IMAGE_BYTES</code></td><td style="text-align:right;"><code>8000000</code></td><td>Maximum Telegram photo download size</td></tr><tr><td><code>TELEGRAM_MEDIA_MAX_AUDIO_BYTES</code></td><td style="text-align:right;"><code>10000000</code></td><td>Maximum voice/audio download size</td></tr><tr><td><code>TELEGRAM_MEDIA_MAX_AUDIO_SECONDS</code></td><td style="text-align:right;"><code>120</code></td><td>Maximum voice/audio duration</td></tr></tbody></table><p>When the AI manager is selected, conversation, task routing, and ambiguous incident matching use the same inventory-declared private host and shared circuit breaker. The endpoint path is fixed to <code>/v1/chat/completions</code>, redirects are disabled, and OpenAI is used after transport, HTTP, malformed response, or schema-validation failures. The OpenAI API key is never sent to the LAN host.</p><p>When Ollama is selected, connection failures, timeouts, HTTP errors, malformed responses, and schema-validation failures open a process-local circuit breaker and route the current request to Luna. No tool is executed until a model decision has passed schema validation, so fallback cannot duplicate tool execution. The endpoint is operator configuration, never caller input, and must only be exposed over the trusted private network.</p><p>When OpenCode Go is selected, the API calls the documented fixed HTTPS Chat Completions endpoint directly with the reviewed <code>deepseek-v4-flash</code> model. Callers cannot choose the endpoint or an arbitrary model; redirects are disabled, retries are bounded to transient failures, and the result is validated again before any tool execution. OpenCode Go failures fall back to the inventory-bound AI manager, which retains its existing Luna fallback. Each fallback happens before a validated decision can request a governed tool, so it cannot replay tool execution. Failed turns store normalized telemetry without prompts, raw output or the API key. OpenCode documents 30-day retention for Grok 4.5; choose the closed-enum DeepSeek rollback only if its latency/quality is acceptable and zero-retention is required. See ADR 0020.</p><p>Telegram free text uses Telegram&#39;s native typing indicator while inference is running, then sends the final reply with the existing navigation callbacks. The home screen exposes an <strong>Operations</strong> panel instead of coupling the feature name to a model provider. Summary buttons map to fixed <code>lab.*.summary</code> tools; <strong>Ask live question</strong> sends a new <code>ForceReply</code> prompt and automatically returns to tool-free chat after its single response. The webhook keeps a bounded, process-local cache of completed Telegram <code>update_id</code> values so ordinary retries do not duplicate a turn or progress message. A savepoint rolls back task-tool database effects if the follow-up model decision fails. The cache is not a cross-process or restart-persistent idempotency guarantee: an update replayed after an API restart can be processed again, so persistent webhook idempotency remains a separate hardening item.</p><p>Token usage and estimated cost are stored on assistant messages. Cost estimates come from <code>CONVERSATION_INPUT_COST_PER_MILLION</code> and <code>CONVERSATION_OUTPUT_COST_PER_MILLION</code>; leave them at <code>0</code> if unknown. The REST response includes the model, input/output token counts, estimated cost, and the summary tools used for each assistant response so an operator or future UI can validate whether the answer came from live control-panel data.</p><p>Transient OpenAI failures (<code>408</code>, <code>409</code>, <code>429</code>, and <code>5xx</code>) are retried briefly. Non-transient API errors such as invalid schema/model/input failures are not retried, so task creation and confirmations are not duplicated.</p><h2 id="task-creation-flow" tabindex="-1">Task Creation Flow <a class="header-anchor" href="#task-creation-flow" aria-label="Permalink to &quot;Task Creation Flow&quot;">​</a></h2><p>If the operator explicitly asks to create a task (&quot;apri una task&quot;, &quot;crea ticket&quot;, etc.), the backend creates it immediately using the task service. When <code>TASK_ROUTER_ENABLED=true</code>, the created task and a durable routing job are committed together. The asynchronous Task Router later appends an advisory <code>task.router_decision</code> timeline event; conversation latency no longer includes router inference. The router does not confirm, claim, assign, or execute the task. <code>TASK_ROUTER_PROVIDER</code> can override the conversation provider; when empty it inherits <code>CONVERSATION_PROVIDER</code>.</p><p>If the model only thinks a task would be useful, the backend stores a pending task proposal instead:</p><ol><li>the assistant replies with the proposal;</li><li>the web UI shows a confirmation action, or Telegram shows an inline button;</li><li>the operator confirms via <code>POST /api/conversations/task-proposals/{nonce}/confirm</code> or the Telegram callback;</li><li>only then is the task created.</li></ol><p>Pending proposals are nonce-protected, single-use, and expire after 15 minutes. Confirmed proposals follow the same asynchronous Task Router enrichment path as explicit task creation.</p><h2 id="migration" tabindex="-1">Migration <a class="header-anchor" href="#migration" aria-label="Permalink to &quot;Migration&quot;">​</a></h2><p>The conversation tables are part of the Alembic-managed schema:</p><ul><li><code>conversations</code></li><li><code>conversation_messages</code></li></ul><p>For a fresh live database, <code>alembic upgrade head</code> creates both tables alongside the rest of the control-plane schema. PostgreSQL is the only supported database for the API in development, tests and live operation.</p>`,49)])])}const m=t(i,[["render",n]]);export{u as __pageData,m as default};
