import{_ as t,o as a,c as o,a2 as r}from"./chunks/framework.BxL9yGGP.js";const u=JSON.parse('{"title":"Security","description":"","frontmatter":{},"headers":[],"relativePath":"security.md","filePath":"security.md","lastUpdated":null}'),n={name:"security.md"};function i(s,e,d,c,l,h){return a(),o("div",null,[...e[0]||(e[0]=[r('<h1 id="security" tabindex="-1">Security <a class="header-anchor" href="#security" aria-label="Permalink to &quot;Security&quot;">​</a></h1><h2 id="threat-model" tabindex="-1">Threat model <a class="header-anchor" href="#threat-model" aria-label="Permalink to &quot;Threat model&quot;">​</a></h2><p>Homelab Console currently runs on a <strong>home application host inside the private home network</strong>, behind Caddy. A separate VPS runs External Sentinel and receives outbound heartbeats over a narrowly routed WireGuard peer. That framing drives most of the design:</p><ul><li>The API is internet-reachable (behind a reverse proxy), so it must assume hostile, unauthenticated traffic on every public endpoint at all times.</li><li>A compromise of the home application host application host would occur inside the home network, so provider reach is constrained to typed adapters, credentials remain outside model surfaces, and the tool plane exposes no generic lateral-movement capability.</li><li>Providers are reached directly over the trusted LAN through typed adapters. The WireGuard peer is reserved for the inventory-bound API-lifecycle Sentinel heartbeat and deployment traffic and does not route the home LAN. The live catalog is read-only; any future write capability must be individually approved and never becomes a general tunnel, SSH gateway, or arbitrary-URL proxy.</li><li>The operator&#39;s Telegram account is a security-relevant identity (second factor by default, optional sole login factor, approvals and model switching), so the bot validates by numeric user ID and chat ID, never by username, and every callback carries a short-lived single-use nonce.</li><li>AI/model surfaces are treated as semi-trusted: MCP clients such as Claude, Codex and Cline can request tool calls, and the Conversation Service can request a narrow summary/task allowlist. Both paths are assumed to potentially be manipulated (prompt injection, confused deputy). They never hold credentials and every infrastructure tool call still passes through authn, risk policy, approval, redaction, and audit.</li></ul><h2 id="trust-boundaries" tabindex="-1">Trust boundaries <a class="header-anchor" href="#trust-boundaries" aria-label="Permalink to &quot;Trust boundaries&quot;">​</a></h2><p>See the ASCII diagram in <a href="./architecture#trust-boundaries"><code>architecture.md</code></a>. In short: Internet → Cloudflare Tunnel/reverse proxy → loopback API → typed provider adapters over the trusted LAN. A separate outbound WireGuard path carries the fixed ADR 0023 heartbeat from the API lifecycle to the VPS. The API, database, MCP origin and provider APIs are never directly exposed. Under ADR 0017, the dedicated MCP hostname reaches the LAN MCP origin only through Cloudflare Tunnel and still requires a per-client bearer token on every request.</p><h2 id="public-endpoints" tabindex="-1">Public endpoints <a class="header-anchor" href="#public-endpoints" aria-label="Permalink to &quot;Public endpoints&quot;">​</a></h2><p>Only these endpoints are reachable without an authenticated session:</p><ul><li><code>GET /health</code></li><li><code>GET /api/auth/config</code></li><li><code>POST /api/auth/login</code></li><li><code>GET /api/auth/challenge/{id}</code></li><li><code>POST /api/auth/complete</code></li><li><code>POST /api/auth/verify-otp</code></li><li><code>POST /api/auth/recovery</code> (when recovery is enabled)</li><li><code>POST /api/telegram/webhook</code> (protected by <code>TELEGRAM_WEBHOOK_SECRET</code>)</li><li><code>POST /api/mcp/pairing/start</code> (rate-limited; starts Telegram approval)</li><li><code>POST /api/mcp/pairing/consume</code> (requires the one-time pairing secret)</li></ul><p>Every other API endpoint requires a valid server-side session cookie. MCP HTTP itself is exposed on the configured MCP endpoint and requires a per-client bearer token issued through Telegram pairing.</p><p><code>AUTH_LOGIN_MODE=password</code> remains the default. The operator-approved <code>telegram_only</code> mode treats the submitted username as account identification, not a secret, and makes Telegram approval/OTP the sole factor. It preserves per-IP, per-username and per-account challenge limits, rejects password fields, and disables password recovery codes. This mode increases the impact of a Telegram account, bot-token, webhook-secret or allowed-ID compromise and must be rolled back to <code>password</code> if that identity boundary is in doubt.</p><p>MCP HTTP access is client-scoped: each approved client gets its own token, agent id (<code>claude</code>, <code>fixer</code>, <code>codex</code>, <code>cline</code>, <code>opencode</code>, or <code>worker</code>), label, fingerprint, dashboard row, revocation state, and <code>last_seen_at</code>. Individual MCP tool calls cannot choose or spoof the agent identity; it is derived from the bearer token record. The public MCP hostname does not use Cloudflare Access: possession of an operator-approved MCP bearer token is the application authentication boundary. Pairing remains Telegram-approved, tokens are stored only as hashes server-side, and revocation takes effect on the next request.</p><h2 id="secret-handling-and-redaction" tabindex="-1">Secret handling and redaction <a class="header-anchor" href="#secret-handling-and-redaction" aria-label="Permalink to &quot;Secret handling and redaction&quot;">​</a></h2><ul><li>Provider credentials (e.g. Proxmox API token) live only in <code>SECRETS_PATH</code> (default <code>config/secrets.local.yml</code>), which is gitignored and never committed. The repo only ships an example file documenting the expected shape, with empty/placeholder values.</li><li>Provider endpoints, hostnames, ports, timeouts and TLS policy live in <code>HOMELAB_CONFIG_PATH</code> (use a gitignored <code>config/homelab.local.yml</code> for real local values). Do not put <code>base_url</code>, <code>host</code>, <code>port</code>, or <code>verify_tls</code> in the secrets file.</li><li>TLS verification remains enabled by default. The explicit <code>ALLOW_INSECURE_LOCAL_TLS=true</code> exception permits <code>verify_tls: false</code> only for private-IP provider endpoints on a trusted LAN/VPN, logs a warning, and never permits the exception for a public endpoint.</li><li>Secrets are read by provider adapters; they are not stored in the database, not put in task/finding/artifact data, and not passed to model providers.</li><li>Task titles, goals, summaries, notes, findings and checks are application state. They are length-limited, schema-validated and redacted before event or audit persistence; they are not a place for credentials.</li><li>The execution core applies <strong>centralized redaction</strong> to every tool response and every audit event before it is persisted or returned. Keys matching (case-insensitively) <code>password</code>, <code>secret</code>, <code>token</code>, <code>api_key</code>, <code>authorization</code>, <code>cookie</code>, <code>session</code>, <code>otp</code>, <code>csrf</code>, <code>credential</code> are redacted, plus provider-specific rules for known credential shapes.</li><li>Redaction happens before the audit log write and before the response leaves <code>execute_tool</code>, so neither the DB, the optional JSONL audit sink, nor any caller (REST, MCP, or a model) ever sees an unredacted secret through the tool plane.</li></ul><h2 id="what-ai-models-never-see" tabindex="-1">What AI models never see <a class="header-anchor" href="#what-ai-models-never-see" aria-label="Permalink to &quot;What AI models never see&quot;">​</a></h2><ul><li>Provider credentials, API tokens, or the contents of <code>SECRETS_PATH</code>.</li><li>Raw local provider endpoint configuration from <code>HOMELAB_CONFIG_PATH</code>.</li><li>Session cookies, CSRF tokens, or other auth material.</li><li>Raw, unredacted tool output — models see the same redacted output the execution core returns to any other caller.</li><li>Anything outside the declared tool schemas: models cannot request arbitrary shell commands, arbitrary HTTP calls, or arbitrary SSH sessions, because no such tool exists.</li><li>Conversation history is bounded and channel-neutral. The Conversation Service receives only compact recent messages, allowed-tool metadata, an optional current task, and redacted summary results; it does not receive full audit logs, provider config files, or unbounded transcripts.</li><li>The optional Task Router receives only compact redacted task context and stores an advisory <code>task.router_decision</code> event. Its durable queue contains only that redacted context. The direct OpenCode Go client has a fixed HTTPS origin, fixed reviewed model mapping, no redirects and no MCP identity. It cannot call tools, claim tasks, set <code>assigned_agent</code>, change task status, or resolve incidents. Its API key stays in the API runtime environment and is never stored in task events, usage rows or audit metadata.</li><li>The LAN AI manager receives only bounded redacted decision context. Its address comes from a private literal-IP inventory host, its port must be declared for that host, its path is fixed, and redirects are disabled. It has no MCP credentials or direct infrastructure capability; output remains untrusted until backend schema validation succeeds.</li></ul><h2 id="ssrf-policy" tabindex="-1">SSRF policy <a class="header-anchor" href="#ssrf-policy" aria-label="Permalink to &quot;SSRF policy&quot;">​</a></h2><p>There is no tool or code path that performs a health check or request against an arbitrary, caller-supplied URL. All provider/host reachability checks resolve their target from an <strong>inventory ID</strong> (a known host/service already present in <code>HOMELAB_CONFIG_PATH</code>), not from free-form input, and are bounded to expected ports and short timeouts. This closes the classic &quot;paste a URL, have the server fetch it&quot; SSRF vector by construction — the input space for a target is a fixed, pre-configured ID list, not a string. The AI manager follows the same rule and additionally rejects public IPs, hostnames, and ports absent from the selected inventory host.</p><h2 id="risk-levels-and-tool-policy" tabindex="-1">Risk levels and tool policy <a class="header-anchor" href="#risk-levels-and-tool-policy" aria-label="Permalink to &quot;Risk levels and tool policy&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Risk</th><th>Policy this milestone</th></tr></thead><tbody><tr><td><code>low</code> (read)</td><td>Any authenticated user</td></tr><tr><td><code>medium</code></td><td>Requires an explicit policy grant</td></tr><tr><td><code>high</code></td><td>Requires approval; <strong>disabled by default</strong></td></tr><tr><td><code>critical</code></td><td>Not implemented</td></tr><tr><td>infrastructure <code>write</code> tool</td><td>Requires exact operator allowlisting, an ADR and confirmation</td></tr><tr><td>task-state write</td><td>Allowed only through typed task service operations, subject to <a href="#task-ownership">task ownership</a></td></tr></tbody></table><p>The system now has a deliberately small infrastructure write surface: only exact tools present in <code>APPROVED_WRITE_TOOLS</code> can be enabled, and every invocation consumes a single-use, input-bound operator approval. Task-state writes (<code>tasks.claim</code>, <code>tasks.add_finding</code>, <code>tasks.complete</code>, etc.) modify only Homelab Console&#39;s own database and create audit/task events. Enabling an infrastructure write tool is a deliberate, reviewed step backed by an exact machine-readable allowlist entry and ADR — not a configuration toggle an operator or model can flip at runtime. The current machine-readable allowlist is authoritative; new Proxmox LXC, OPNsense WoL and OPNsense gateway-transition tools were activated under ADRs 0006, 0008 and 0010 after their scoped live approval drills. The unsuccessful egress-switch drill recorded by ADR 0011 ended with rollback and removal of that tool from governance.</p><p>High-risk approvals are single-use at the database boundary: the execution core consumes an <code>approved</code> row with one conditional update, and <code>tool_invocations.approval_id</code> is unique. Concurrent calls using the same approval therefore cannot both reach the provider. Infrastructure write tools remain disabled unless separately approved despite this defence.</p><h2 id="task-ownership" tabindex="-1">Task ownership <a class="header-anchor" href="#task-ownership" aria-label="Permalink to &quot;Task ownership&quot;">​</a></h2><p>Once a task is claimed, <code>app.services.tasks_service</code> enforces that only the claiming agent (<code>agent:claude</code>, <code>agent:fixer</code>, <code>agent:codex</code>, <code>agent:cline</code>, <code>agent:opencode</code>, or a per-client <code>agent:worker:&lt;client-id&gt;</code> principal) can mutate it further (<code>not_task_owner</code> otherwise); a human operator acting through the authenticated web UI or Telegram always bypasses this (administrative override), and an agent may not touch a task that hasn&#39;t been claimed yet either.</p><p>This is a coordination and audit-hygiene control backed by the MCP client registry: for HTTP clients, the agent identity comes from the approved per-client token record. Revoke a client token from the MCP dashboard when a machine, tool, or local config should no longer access the control plane.</p><p>The optional legacy OpenCode Fixer uses the separate <code>agent:fixer</code> identity. Before the API contacts its loopback supervisor, it records and verifies an exact <code>task.fixer_dispatch_requested</code> event bound to the task, actor, owner and dispatchable state. The static dispatch secret authenticates process launch; the dedicated MCP token, task ownership checks and execution core independently control what that process can read or change. Legacy <code>claude-fixer</code> clients are revoked during deployment rather than accepted through a compatibility alias. ADR 0024 replaces this with per-client remediation worker principals, operator-granted capability and durable lease fencing. Core enforcement binds task mutation, approval request/consumption and task-bound infrastructure calls to the authenticated client, worker job and current lease generation. A core-owned recovery loop fences expired workers and releases exhausted jobs. The legacy boundary remains available only for rollback until an external pull adapter passes conformance, and must never process the same task concurrently. Granting <code>task-worker.v1</code> permanently converts that MCP registration to a unique worker principal. Removing the capability does not downgrade its bearer token back to the shared interactive agent family. Revocation closes active jobs and releases non-final tasks; registrations with worker history remain audit tombstones and cannot be forgotten.</p><p>All status changes, including claim/release and guarded watcher auto-completion, pass through one transition helper and append the canonical <code>task.status_changed</code> event. Final tasks (<code>completed</code>/<code>cancelled</code>) reject summary, note, finding, check and task-linked tool mutations with <code>task_not_active</code>; reopening is the only supported way to make them mutable again. The operator override bypasses ownership, not final-state immutability.</p><h2 id="non-goals-explicit" tabindex="-1">Non-goals (explicit) <a class="header-anchor" href="#non-goals-explicit" aria-label="Permalink to &quot;Non-goals (explicit)&quot;">​</a></h2><ul><li><strong>Not a general remote-access tool.</strong> No arbitrary shell, SSH, or HTTP proxying into the homelab, now or planned.</li><li><strong>Not multi-tenant.</strong> Designed for a single operator/household, not a shared or public service.</li><li><strong>SMS is explicitly deferred</strong> as a second factor; Telegram approval/OTP is the only supported second factor today, with recovery codes as the offline fallback.</li><li><strong>Not a secrets manager.</strong> <code>SECRETS_PATH</code> is a local file read by the app; there is no vault integration in this milestone.</li><li><strong>Not defended against a fully compromised VPS host.</strong> WireGuard, disk encryption, and OS hardening at the host level are out of scope for the application itself and assumed to be handled operationally (see operator deployment guide).</li><li><strong>The <code>/models</code> endpoints are operator-scoped only, decoupled from tasks.</strong> <code>assigned_agent</code> (set exclusively by <code>tasks.claim</code>) is the only field that determines who owns a task; the &quot;active model&quot; concept plays no role in task ownership or the task lifecycle.</li><li><strong>Conversation is not an agent runner.</strong> It does not start MCP clients, does not run autonomous loops, and cannot expand its own tool catalog. It can answer operator questions, call narrow summary/task tools, and propose or create tasks under the rules in <a href="./conversation"><code>conversation.md</code></a>.</li><li><strong>Task Router is not an owner or dispatcher.</strong> It can classify newly created tasks and suggest an owner/runbook in a timeline event, but only <code>tasks.claim</code> changes <code>assigned_agent</code>, and only typed task-service operations mutate task state.</li></ul>',29)])])}const m=t(n,[["render",i]]);export{u as __pageData,m as default};
