import{_ as a,o as n,c as t,a2 as s}from"./chunks/framework.DYd_c05j.js";const h=JSON.parse('{"title":"Architecture","description":"","frontmatter":{},"headers":[],"relativePath":"architecture.md","filePath":"architecture.md","lastUpdated":null}'),o={name:"architecture.md"};function r(i,e,l,d,c,p){return n(),t("div",null,[...e[0]||(e[0]=[s(`<h1 id="architecture" tabindex="-1">Architecture <a class="header-anchor" href="#architecture" aria-label="Permalink to &quot;Architecture&quot;">​</a></h1><h2 id="the-five-planes" tabindex="-1">The five planes <a class="header-anchor" href="#the-five-planes" aria-label="Permalink to &quot;The five planes&quot;">​</a></h2><p>Homelab Console is organized around five planes with distinct trust levels.</p><ul><li><strong>Control plane</strong> — everything about the system&#39;s own state: inventory, users, sessions, login challenges, provider configuration, tool definitions, task state, findings/evidence, model selection, approvals, and the audit log. Owns the database.</li><li><strong>Tool plane</strong> — the only way anything (human or model) can act on homelab infrastructure. A fixed set of narrow, explicitly-named tools (e.g. <code>proxmox.nodes.list</code>), each with its own input/output schema and risk level. There is no generic shell, SSH, HTTP, or API forwarding tool — every capability is declared, typed, and reviewable.</li><li><strong>Model plane</strong> — configured model providers power the lightweight Conversation Service used by Telegram free text and authenticated REST conversation endpoints, plus the optional Task Router enrichment pass. Models receive compact, redacted context only and cannot reach providers directly. This path is for operator Q&amp;A, task proposals, and task classification; it is not an MCP agent runner.</li><li><strong>Watcher plane</strong> — optional scheduled detection that calls only read-only summary tools through <code>execute_tool</code>, records incidents, and creates deduplicated tasks. It does not run models, execute fixes, or bypass the tool plane.</li><li><strong>Operator plane</strong> — how a human reaches the system: the Win98-style web desktop (apps/web), the Telegram bot (status, approvals, model switching), and MCP clients (apps/mcp) speaking the same tool surface over stdio or HTTP.</li></ul><h2 id="shared-execution-core" tabindex="-1">Shared execution core <a class="header-anchor" href="#shared-execution-core" aria-label="Permalink to &quot;Shared execution core&quot;">​</a></h2><p>Both the REST API and the MCP adapter call into the same execution core — there is exactly one code path that can run a tool, regardless of which plane initiated the request:</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>execute_tool(tool_id, validated_input, actor, source, task_id, approval_id)</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>Pipeline, in order:</p><ol><li><strong>Tool lookup</strong> — resolve <code>tool_id</code> against the tool registry.</li><li><strong>Enabled check</strong> — reject if the tool is disabled. Unapproved write tools are forced disabled by registry governance even if configuration attempts to enable them; see current milestone policy.</li><li><strong>Input validation</strong> — Pydantic schema validation; extra/unknown fields are rejected, not silently dropped.</li><li><strong>Authentication</strong> — resolve and verify the calling actor (session or MCP per-client token).</li><li><strong>Risk-policy check</strong> — evaluate the tool&#39;s declared risk level against policy (see below).</li><li><strong>Approval verification</strong> — for tools that require confirmation, verify a matching, unconsumed <code>Approval</code> record.</li><li><strong>Timeout enforcement</strong> — bound provider calls by the tool&#39;s <code>timeout_seconds</code>.</li><li><strong>Provider invocation</strong> — call the underlying provider adapter.</li><li><strong>Output normalization</strong> — coerce the provider&#39;s response into the tool&#39;s declared output schema.</li><li><strong>Secret redaction</strong> — strip anything matching sensitive-key patterns before it leaves this function (see <a href="./security">security.md</a>).</li><li><strong>Audit log</strong> — write a structured <code>AuditEvent</code>.</li><li><strong>Task event</strong> — append a <code>TaskEvent</code> if the call happened in the context of a task.</li><li><strong>Response</strong> — return the normalized, redacted result to the caller.</li></ol><p>Because REST and MCP both terminate in this same function, there is no &quot;MCP-only&quot; or &quot;REST-only&quot; capability, and no way to bypass validation, policy, approval, or redaction by picking one transport over the other.</p><h2 id="tool-definitions" tabindex="-1">Tool definitions <a class="header-anchor" href="#tool-definitions" aria-label="Permalink to &quot;Tool definitions&quot;">​</a></h2><p>Each tool declares:</p><table tabindex="0"><thead><tr><th>Field</th><th>Meaning</th></tr></thead><tbody><tr><td><code>id</code></td><td>Stable dotted identifier, e.g. <code>proxmox.nodes.list</code></td></tr><tr><td><code>name</code>, <code>description</code></td><td>Human-readable metadata</td></tr><tr><td><code>provider_id</code></td><td>Which provider adapter implements it</td></tr><tr><td><code>category</code></td><td>Grouping for UI/discovery</td></tr><tr><td><code>mode</code></td><td><code>read</code> or <code>write</code></td></tr><tr><td><code>risk</code></td><td><code>low</code>, <code>medium</code>, <code>high</code>, or <code>critical</code></td></tr><tr><td><code>enabled</code></td><td>Whether the tool can currently run</td></tr><tr><td><code>timeout_seconds</code></td><td>Hard timeout enforced by the execution core</td></tr><tr><td><code>requires_confirmation</code></td><td>Whether an <code>Approval</code> is required before running</td></tr><tr><td>input/output schemas</td><td>Pydantic models, extra fields rejected</td></tr></tbody></table><p>Risk policy this milestone:</p><ul><li><strong>low</strong>-risk read tools run for any authenticated user.</li><li><strong>medium</strong>-risk tools require an explicit policy grant.</li><li><strong>high</strong>-risk tools require approval and are <strong>disabled by default</strong>.</li><li><strong>critical</strong>-risk tools are not implemented.</li><li>Write tools require confirmation and remain disabled unless their exact id has operator approval in <code>app.tools.governance.APPROVED_WRITE_TOOLS</code> backed by an ADR. The current allowlist contains AdGuard protection pause/resume under ADR 0004, Proxmox LXC start/shutdown under ADR 0006, OPNsense WoL under ADR 0008, and OPNsense gateway failover/restore under ADR 0010. The egress switch attempted under ADR 0011 is not allowlisted.</li></ul><h2 id="entity-model" tabindex="-1">Entity model <a class="header-anchor" href="#entity-model" aria-label="Permalink to &quot;Entity model&quot;">​</a></h2><p>Core persisted entities (owned by the control plane):</p><ul><li><code>User</code>, <code>Session</code>, <code>LoginChallenge</code> — identity and auth state.</li><li><code>ProviderConfiguration</code> — per-provider connection settings and status.</li><li><code>Task</code>, <code>TaskEvent</code> — units of operator/model work and their timeline.</li><li>Task lifecycle — all state changes use the shared transition helper and append <code>task.status_changed</code>; claim/release and watcher events add context but do not replace that canonical event. Final tasks are immutable until explicitly reopened.</li><li><code>ToolInvocation</code> — one record per <code>execute_tool</code> call.</li><li><code>Finding</code> — structured evidence produced while working a task.</li><li><code>Conversation</code>, <code>ConversationMessage</code> — bounded operator conversation history, model usage, and estimated cost for Telegram/REST chat. This state is not a replacement for tasks.</li><li>Task Router — optional asynchronous model pass backed by a durable one-job-per-task queue. It classifies newly created tasks and appends a <code>task.router_decision</code> event. It does not own, claim, execute, close or mutate tasks beyond that auditable timeline event.</li><li><code>WatcherRun</code>, <code>Incident</code> — automatic read-only detection history and deduplicated issue records linked to watcher-created tasks.</li><li>Task Context Compiler — deterministic read model that compiles a task, linked incident, recent evidence, provider state, relevant read-only tools and budget into the minimum brief passed to MCP/agent surfaces.</li><li><code>Approval</code> — explicit confirmation records for tools that require them.</li><li><code>McpClient</code>, <code>McpPairingRequest</code> — persistent, revocable MCP client registrations. Pairing is approved over Telegram; only token hashes are stored server-side.</li><li><code>AuditEvent</code> — structured, redacted audit trail.</li><li><code>ModelProfile</code> — configured Conversation Service model providers and the currently selected one. MCP task ownership is tracked separately through <code>assigned_agent</code>.</li></ul><p>Task state, findings, evidence, and tool history are all persisted in the database and survive a model-provider switch; only the reasoning engine changes.</p><h2 id="conversation-service" tabindex="-1">Conversation Service <a class="header-anchor" href="#conversation-service" aria-label="Permalink to &quot;Conversation Service&quot;">​</a></h2><p>Telegram free text and the authenticated <code>/api/conversations/*</code> endpoints share one channel-neutral Conversation Service. The desktop Chat app is not currently registered in the web UI. The service can call only a small allowlist of summary and task tools, and summary-tool execution still goes through the shared <code>execute_tool</code> core. It receives compact context only: the current user message, bounded recent history, the allowed tool catalog, an optional current task, and redacted summary results.</p><p>The Conversation Service uses strict structured output and the backend decides what is actually executed. It may create a task immediately only when the operator explicitly asks for one; otherwise it stores a pending task proposal that requires an operator confirmation button. See <a href="./conversation"><code>conversation.md</code></a> for the concrete contract and limits.</p><p>When <code>CONVERSATION_PROVIDER=ai_manager</code>, the Conversation Service, Task Router, and Incident Matcher send bounded redacted context to one private literal IP resolved from the declared <code>ai-host</code> inventory host. The OpenAI-compatible path is fixed, redirects are disabled, strict model output is validated before execution, and a shared circuit breaker routes failures to Luna. The LAN model has no MCP identity or direct tool access.</p><p>When <code>CONVERSATION_PROVIDER=ollama</code>, the backend sends bounded conversation context to one operator-configured private Ollama origin. Connection failures, timeouts, HTTP failures and invalid structured output open a short process-local circuit breaker and route the turn to Luna. Tool execution begins only after a valid decision, so fallback cannot duplicate an infrastructure call. The endpoint and model are server configuration, never caller input.</p><p>Authorized Telegram photo and audio messages follow a separate bounded path:</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Telegram file_id</span></span>
<span class="line"><span>  -&gt; fixed Telegram getFile/download origins</span></span>
<span class="line"><span>  -&gt; byte and duration validation</span></span>
<span class="line"><span>  -&gt; local Ollama image analysis or audio transcription</span></span>
<span class="line"><span>  -&gt; bounded text</span></span>
<span class="line"><span>  -&gt; shared Conversation Service</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br></div></div><p>Original media bytes and base64 are not written to conversation history, audit metadata or model-usage records and are never sent to Luna. Audio decoding is in-process with fixed mono 16 kHz PCM output; no shell command, caller-selected path, URL, codec or conversion option is accepted.</p><h2 id="task-router" tabindex="-1">Task Router <a class="header-anchor" href="#task-router" aria-label="Permalink to &quot;Task Router&quot;">​</a></h2><p>The Task Router is an optional asynchronous model pass over newly created tasks. REST, provider entrypoints, watcher incidents and confirmed Conversation Service task creation enqueue a durable job in the same transaction as the task; model inference happens only after that transaction commits. Its output is intentionally advisory: category, priority, severity, suggested owner, labels, possible runbook, dedupe candidate, confidence, and first read-only steps.</p><p>The router never calls tools, never assigns <code>assigned_agent</code>, never changes task status, and never resolves incidents. The only persisted side effects are a redacted <code>task.router_decision</code> task event and a matching audit event. If the model is disabled or fails, task creation continues normally.</p><p>With <code>TASK_ROUTER_PROVIDER=opencode_go</code>, inference uses the fixed OpenCode Go HTTPS endpoint and reviewed <code>deepseek-v4-pro</code> model. The client has no MCP identity or tool surface, rejects redirects and validates the result again. Stale queue leases are recovered after a bounded interval; the unique task/job constraint prevents duplicate routing. ADR 0024 defines the migration of autonomous remediation to a separate vendor-neutral MCP worker role. The core owns capability grants, assignment, durable leases, recovery and fencing while external OpenCode, Codex or future adapters own only their isolated agent runtime. The legacy OpenCode Fixer remains a rollback boundary until the first external pull adapter passes conformance.</p><p>The <strong>Metrics</strong> window adds immutable per-component usage rows and explicit operator reviews. Technical success, reviewed accuracy and review coverage remain separate metrics. Token-based cost is stored with the price snapshot used at the time and is labelled attributed cost, not invoice-reconciled spend. See <a href="./metrics"><code>metrics.md</code></a>.</p><p>The inventory-bound AI manager permits one in-process inference at a time and records queue wait, inference latency, effective provider/model, fallback, sanitized error category, and prompt/schema/model versions on the same canonical usage row. No prompt or raw output is added to metrics telemetry.</p><h2 id="watchers" tabindex="-1">Watchers <a class="header-anchor" href="#watchers" aria-label="Permalink to &quot;Watchers&quot;">​</a></h2><p>Watchers are an opt-in detection layer. The first watcher, <code>lab.alerts</code>, runs <code>lab.alerts.recent</code>, extracts warning/critical findings, and creates one task per new open incident. Repeated sightings update the same incident instead of creating duplicate tasks.</p><p>Watchers use <code>execute_tool</code> for every infrastructure observation and <code>tasks_service.create_task</code> for task creation. They do not call MCP agents, the Conversation Service, shell, SSH, arbitrary HTTP, or any write-mode provider tool. See <a href="./watchers"><code>watchers.md</code></a>.</p><h2 id="external-sentinel" tabindex="-1">External Sentinel <a class="header-anchor" href="#external-sentinel" aria-label="Permalink to &quot;External Sentinel&quot;">​</a></h2><p>Watchers detect problems from the console&#39;s own home application host/home-network vantage point, so they are structurally blind when that host, site or outbound path is unreachable. External Sentinel is a small, independently released VPS-side service that covers exactly that gap: fixed-config HTTP health checks plus a heartbeat receiver, local SQLite incident dedup, and direct Telegram alerting. Its source, tests and deployment assets live in <a href="https://github.com/imdelmare/homelab-console-sentinel" target="_blank" rel="noreferrer"><code>imdelmare/homelab-console-sentinel</code></a>.</p><p>It is deliberately outside the five-plane model above rather than an extension of it: it never calls <code>execute_tool</code> or any Homelab Console API, holds no infrastructure credentials, and cannot reach providers, agents, models, or the tool plane. Its own attack surface is limited to the heartbeat receiver, which requires a bearer/token match (<code>SENTINEL_HEARTBEAT_TOKEN</code>) and fails closed — a missing or wrong token is always rejected, never treated as &quot;no auth configured, allow it&quot;. See <a href="./sentinel"><code>sentinel.md</code></a>.</p><h2 id="trust-boundaries" tabindex="-1">Trust boundaries <a class="header-anchor" href="#trust-boundaries" aria-label="Permalink to &quot;Trust boundaries&quot;">​</a></h2><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>                         Internet (untrusted)</span></span>
<span class="line"><span>                                │</span></span>
<span class="line"><span>                                ▼</span></span>
<span class="line"><span>                    ┌───────────────────────┐</span></span>
<span class="line"><span>                    │  Reverse proxy (HTTPS) │   TLS termination,</span></span>
<span class="line"><span>                    │        Caddy           │   security headers</span></span>
<span class="line"><span>                    └───────────┬───────────┘</span></span>
<span class="line"><span>                                │  loopback / private net only</span></span>
<span class="line"><span>                                ▼</span></span>
<span class="line"><span>   ┌────────────────────────────────────────────────────┐</span></span>
<span class="line"><span>   │       home application host: Homelab Console API + database    │</span></span>
<span class="line"><span>   │        (home LAN — session/CSRF/authn boundary)      │</span></span>
<span class="line"><span>   │                                                        │</span></span>
<span class="line"><span>   │   Operator plane ── Control plane ── Tool plane        │</span></span>
<span class="line"><span>   │        │                 │               │            │</span></span>
<span class="line"><span>   │        │                 │        (execute_tool core)  │</span></span>
<span class="line"><span>   │        │                 │               │            │</span></span>
<span class="line"><span>   │   Model/agent surfaces — no credentials, no direct     │</span></span>
<span class="line"><span>   │   provider access, only typed tool requests            │</span></span>
<span class="line"><span>   └───────────────────────────┬─────────────────────────┘</span></span>
<span class="line"><span>                                │  trusted LAN</span></span>
<span class="line"><span>                                ▼</span></span>
<span class="line"><span>                    ┌───────────────────────┐</span></span>
<span class="line"><span>                    │     Home network       │</span></span>
<span class="line"><span>                    │  Proxmox / OPNsense /  │</span></span>
<span class="line"><span>                    │  MikroTik / HA /       │</span></span>
<span class="line"><span>                    │  Frigate (provider     │</span></span>
<span class="line"><span>                    │  APIs, read-only for   │</span></span>
<span class="line"><span>                    │  the real ones today)  │</span></span>
<span class="line"><span>                    └───────────────────────┘</span></span>
<span class="line"><span></span></span>
<span class="line"><span>   ┌────────────────────────────────────────────────────┐</span></span>
<span class="line"><span>   │           External Sentinel (separate VPS)            │</span></span>
<span class="line"><span>   │   independent process, own SQLite, no console API      │</span></span>
<span class="line"><span>   │   access, no infra credentials — Telegram alerting      │</span></span>
<span class="line"><span>   │   only. Receives outbound heartbeat over the dedicated  │</span></span>
<span class="line"><span>   │   WireGuard peer and detects console/site outages.      │</span></span>
<span class="line"><span>   └────────────────────────────────────────────────────┘</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br><span class="line-number">14</span><br><span class="line-number">15</span><br><span class="line-number">16</span><br><span class="line-number">17</span><br><span class="line-number">18</span><br><span class="line-number">19</span><br><span class="line-number">20</span><br><span class="line-number">21</span><br><span class="line-number">22</span><br><span class="line-number">23</span><br><span class="line-number">24</span><br><span class="line-number">25</span><br><span class="line-number">26</span><br><span class="line-number">27</span><br><span class="line-number">28</span><br><span class="line-number">29</span><br><span class="line-number">30</span><br><span class="line-number">31</span><br><span class="line-number">32</span><br><span class="line-number">33</span><br><span class="line-number">34</span><br><span class="line-number">35</span><br><span class="line-number">36</span><br><span class="line-number">37</span><br><span class="line-number">38</span><br></div></div><p>Key boundaries:</p><ul><li><strong>Internet → reverse proxy</strong>: untrusted, TLS-terminated, only exposes the public endpoints listed in <a href="./security">security.md</a>.</li><li><strong>Reverse proxy → API</strong>: loopback only, never directly internet-reachable.</li><li><strong>External Sentinel</strong>: deployed independently of the Homelab Console API (see <a href="./sentinel"><code>sentinel.md</code></a>); it does not sit inside the request path above and has no access to the control/tool/model planes.</li><li><strong>Model plane → tool plane</strong>: models can only request named tool calls through the execution core; they hold no credentials and cannot reach providers directly.</li><li><strong>API → providers</strong>: direct trusted-LAN access only through provider adapters — never a general-purpose tunnel exposed to the app.</li><li><strong>home application host → External Sentinel</strong>: outbound heartbeat over the separately managed WireGuard peer; Sentinel is not in the provider request path.</li></ul><p>See <a href="./security"><code>security.md</code></a> for the threat model this boundary diagram supports, <a href="./authentication"><code>authentication.md</code></a> for how the operator plane&#39;s identity boundary works, and operator deployment guide for the home application host/VPS split deployment.</p>`,44)])])}const m=a(o,[["render",r]]);export{h as __pageData,m as default};
